{
  "dependencies": {
    "@grapesjs/react": "^2.0.0",
    "grapesjs": "^0.22.14"
  },
  "devDependencies": {
    "@babel/generator": "^7.29.1",
    "@babel/parser": "^7.29.0",
    "@babel/traverse": "^7.29.0",
    "@babel/types": "^7.29.0"
  },
  "name": "design2ui",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "frontend",
    "frontend/src/lib/feel-lite"
  ],
  "scripts": {
    "test": "npm test --workspace=packages/schema && npm test --workspace=packages/renderer && npm test --workspace=packages/library && npm test --workspace=packages/editor && npm test --workspace=packages/figma-parser",
    "build:engine-stack": "npm run build --workspace=packages/schema && npm run build --workspace=packages/patches && npm run build --workspace=packages/renderer && npm run build --workspace=packages/library && npm run build --workspace=packages/engine",
    "build:editor-stack": "npm run build --workspace=packages/registry && npm run build --workspace=packages/editor",
    "build": "npm run build:engine-stack && npm run build:editor-stack && npm run build:ir-stack && npm run build:figma-stack",
    "build:ir-stack": "npm run build --workspace=packages/ir && npm run build --workspace=packages/compiler && npm run build --workspace=packages/ahtml && npm run build --workspace=packages/patterns && npm run build --workspace=packages/roundtrip",
    "build:figma-stack": "npm run build --workspace=packages/figma-parser"
  }
}
