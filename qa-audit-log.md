# QA audit log — two-agent workflow

Append-only. Agent A (User-Experience Auditor) logs findings; Agent B
(Fixer/Builder) appends the resolution to the same entry. Never overwrite.

**Project under test:** `gh0mlpbp` (generated Inventory app)
**Editor:** http://localhost:6501/editor/gh0mlpbp
**Generated app (standalone):** http://localhost:6510
**Scaffold preview:** http://localhost:6503/p/gh0mlpbp/&lt;route&gt;

## Scope

43 components across the three categories not covered by audit rounds 1–5,
plus the app's four real routes.

| batch | count | components |
|---|---|---|
| 1 — navigation | 10 | Breadcrumb, CartBadge, CommandPalette, ContextMenu, DropdownMenu, Link, Menubar, NavLink, Redirect, SkipLink |
| 2 — feedback | 21 | Alert, AutoFocus, Banner, Drawer, EmptyState, EmptyStateRich, FocusRing, FocusTrap, HoverCard, IllustratedEmpty, InspectorPanel, LoadingState, OptimisticProvider, Popover, PresenceIndicator, Progress, Skeleton, Spinner, Tooltip, TourOverlay, UndoManager |
| 3 — data | 12 | CartPanel, Chart, Conditional, DataBoundary, DataGrid, EditableLineGrid, Repeat, Slot, Sparkline, Table, TableSortable, Timeline |
| 4 — routes | 4 | `/items`, `/items/new`, `/items/[id]`, `/items/[id]/edit` |

Already covered by rounds 1–5 and excluded here: 18 layout, 41 input,
31 display. Reports in `docs/editor-audit/`.

## Entry format

Each component gets one entry. Agent A writes the first three fields, Agent B
appends the last three.

```
### <Component or route>
- **Bugs found:** repro steps · expected vs actual · severity (blocker/major/minor)
- **Missing/desired features:** what a user expected and did not get
- **Status:** NEEDS_FIX | CLEAN
- **Fix applied:** what changed, files touched
- **Feature added:** what was implemented, files touched
- **Status:** RESOLVED
```

---

# Entries

<!-- Agent A — navigation batch. Scratch page /nav-lab-6 in project gh0mlpbp,
     own Chrome tab. All 10 navigation components dropped from the palette by
     real clicks; evidence is javascript_tool DOM probes on the live canvas. -->

### Breadcrumb
- **Bugs found:** **The Breadcrumb renders perfectly in the editor and is invisible in the real app.** Repro: with the finished breadcrumb on `/nav-lab-6`, open the production renderer at `http://localhost:6503/p/gh0mlpbp/nav-lab-6`. The page body text is `nav lab 6 Search… Ctrl+K Right-click here ActionsLearn moreLink Skip to main content` — **no breadcrumb**. Its two anchors are present in the document but measure **0x0**, and walking their ancestors shows the trail is still sitting inside React's streaming buffer: `<div hidden id="S:0">` whose `textContent` is exactly `Items/Widget A/Edit`. The Suspense boundary that holds it is never swapped into the page — on a full document load the completion script is not emitted at all (`document.querySelectorAll('script:not([src])')` contains **no `$RC(...)` call**, though `window.$RC` is defined). Reproduced on three separate loads. Expected: the crumbs I authored appear in the app. Actual: they exist in the HTML and stay hidden.
  **Refinement worth having:** it is not permanent — after I clicked something else on the page (forcing a client re-render) the breadcrumb *did* appear correctly as `Items / Widget A / Edit`. So the component is fine; **the page's first paint is what drops it.** This is the same failure as the `/items`, `/items/new`, `/items/[id]` and `/items/[id]/edit` route entries below and should be fixed once, in the renderer, not five times. Severity: **major**.
  Everything I did in the *editor* worked. Repro: palette search "Breadcrumb" → click to insert on `/nav-lab-6`. Renders immediately with a real seed (`Home / Current page`, `<nav aria-label="Breadcrumb">`, 960x24). Props panel exposes ITEMS (JSON, marked REQUIRED) and SEPARATOR. Replaced ITEMS with `[{"label":"Items","href":"/items"},{"label":"Widget A","href":"/items/1"},{"label":"Edit"}]` → canvas updated to `Items / Widget A / Edit` with anchors `Items->/items`, `Widget A->/items/1` and the last crumb correctly rendered as plain text, not a link. Typed deliberately broken JSON (`[{"label":"Items", href:/items}`) → panel showed `Not saved — Expected double-quoted property name in JSON at position 19` and the canvas kept the last good value. Clicking the "Home" anchor inside the editor canvas selected the node instead of navigating the editor away — correct. No console errors.
- **Missing/desired features:** the ITEMS editor is a raw JSON textarea with the note "Editing as JSON — no value / key / id on every row"; as a user I wanted a row-per-crumb editor with a label field and a route picker that offers the pages that actually exist in this project (`/items`, `/items/new`, …) instead of me typing route strings by hand and hoping they match. No `currentPageAuto` option either — every breadcrumb has to be hand-authored per page even though the editor knows the page's route.
- **Status:** NEEDS_FIX

### CartBadge
- **Bugs found:** **Renders nothing at all.** Repro: palette search "CartBadge" → click to insert. The node is persisted and appears in the tree as `cartbadge-00zfnb`, but its wrapper has `childElementCount === 0` — `firstElementChild` is `null`, rect 0x0, `innerText` empty. There is no DOM under it whatsoever, and because the box is zero-sized it gets **no empty-node hint overlay either**, so there is nothing on the canvas to click, select or diagnose. This is round 5's Lightbox/Dialog failure (invisible *and* undiagnosable) recurring in a navigation component — same class as round 5 rows 2 and 12. Severity: **major** (a user drops it, sees nothing appear, and has no way to tell whether the drop even worked).
- **Missing/desired features:** I expected a cart icon with a count bubble and a zero/empty state ("0" or a dimmed icon) so it is visible while I lay the page out. Also expected the drop to seed a sample count the way Breadcrumb seeds sample crumbs.
- **Status:** NEEDS_FIX

### `/items` · `/items/new` · `/items/[id]` · `/items/[id]/edit` — standalone app at :6510
- **Bugs found:** **NOT TESTABLE IN BROWSER — the standalone app is down on all four routes.** Repro: open `http://localhost:6510/items` in a fresh tab. `/` 307-redirects, then every one of the four routes returns HTTP 500 with the same server error: `Module not found: Can't resolve './FigmaCanvas'` — `> 19 | import { FigmaCanvas } from "./FigmaCanvas";` — `Import trace for requested module: ./src/app/(dashboard)/[entity]/page.tsx`. Verified in the browser (the Next `_error` payload is in the DOM) and by direct request: `/items` 500 in 1.4s; `/items/new`, `/items/1` and `/items/1/edit` never respond at all (60s timeout each, same module error in the build). Every route of the generated app is behind the `(dashboard)/[entity]` page, so the app has no working page. Severity: **blocker**.
- **Missing/desired features:** as a user, a 500 stack trace about a missing internal module is not something I can act on. n/a otherwise — routes re-audited against the scaffold preview on :6503 in the entries below.
- **Status:** NEEDS_FIX

### ContextMenu
- **Bugs found:** **Right-clicking it opens a completely empty menu.** Repro: insert ContextMenu on `/nav-lab-6` → it renders a dashed 960x48 target reading "Right-click here" → right-click it. A Radix menu opens (`[role="menu"]`, portalled outside the canvas root) measuring **160 x 10 px with `childElementCount === 0`** — a bare white sliver with nothing in it. The reason is visible in the Props panel: the only control the panel offers for this component is **LABEL**. There is no `items` control at all, and the persisted node is `{"type":"ContextMenu","props":{"label":"Right-click here"}}`. So the component's entire purpose — the menu — is unreachable from the editor. Severity: **major**. Same class as round 5 rows 1–8 (the one prop the component exists to show is not in the registry), new instance.
- **Missing/desired features:** an `items` control (label + action per row), and a disabled/destructive row style. I also expected the empty menu to say something like "No menu items" rather than opening a 10px-tall blank box — as a user I could not tell whether it was broken or still loading.
- **Status:** NEEDS_FIX

### DropdownMenu
- **Bugs found:** Two, both verified by clicking.
  1. **Opens an empty menu.** Repro: insert DropdownMenu → renders a 75x34 "Actions" button → click it. A `[role="menu"]` opens measuring **160 x 10 px, `childElementCount === 0`**. Persisted node is `{"type":"DropdownMenu","props":{"trigger":"Actions"}}` — no items, and no items control anywhere in the editor. Severity: **major** (same class as ContextMenu above).
  2. **The node cannot be selected in the editor, so its Props panel is unreachable.** Repro: click the "Actions" button on the canvas → the runtime menu opens and the Properties panel still reads *"Select a node on the canvas to edit its props."* Press Escape, click again → same result, every time. There is no layers/outline panel in this editor (the left rail is Pages only), so once dropped there is **no way to select a DropdownMenu to configure, restyle or delete it**. Contrast: ContextMenu selects correctly on left-click (its menu is bound to right-click), and CommandPalette's trigger button selects correctly. Severity: **major**.
- **Missing/desired features:** an `items` control; and, since the trigger swallows clicks, either a layers panel or the usual design-time convention of suppressing runtime click handlers inside the canvas.
- **Status:** NEEDS_FIX

### CommandPalette
- **Bugs found:** **Its own advertised shortcut does not work — in the editor OR in the real app.** The trigger button literally reads "Search… **Ctrl+K**". Repro in the production renderer (`http://localhost:6503/p/gh0mlpbp/nav-lab-6`): click empty page background, press Ctrl+K → nothing opens (`[role=dialog]` count 0, no `input[placeholder="Search commands…"]`, `activeElement` still BODY). Click the trigger button instead → the palette **does** open, correctly, with the seeded commands grouped as `PAGES / GO TO DASHBOARD` and `ACTIONS / CREATE RECORD`, and typing `dash` filters it down to just "GO TO DASHBOARD". So the palette works; only the keyboard shortcut it advertises on its own face does not. Severity: **major**.
  Two smaller things found while it was open: (a) **focus is not moved into the search box on open** — `document.activeElement` is `BODY`, so a keyboard user has to reach for the mouse before typing; (b) the overlay is a plain `div.fixed.inset-0…` with **no `role="dialog"` and no `aria-modal`**, so it is not announced as a modal (severity: minor each).
  Pressing Enter on the filtered "Go to dashboard" row ran its `navigate → /` action and landed on `http://localhost:6503/` → **404**: the base path `/p/gh0mlpbp` is dropped, the same base-path bug as Redirect below. Browser Back from there returned correctly to the page.
  And, importantly, **it does not steal the editor's keyboard** — this is the round-4/5 trap and it does NOT recur here. Repro: (a) click the editor's own "Search components…" box and press Ctrl+K → nothing opens, focus stays in the editor's input, its value is untouched; (b) click on the canvas and press Ctrl+K → no dialog, `[role=dialog]` count 0. Clicking the component's own "Search… Ctrl+K" trigger button on the canvas selects the node (Props panel shows `CommandPalette / commandpalette-megoq6`) rather than opening the palette — correct design-time behaviour. Props panel exposes ITEMS (JSON, REQUIRED), PLACEHOLDER and TRIGGERKEY, and the drop seeds two real sample commands (`Go to dashboard` → navigate `/`, `Create record` → workflow `createRecord`) so it is not blank on arrival.
- **Missing/desired features:** the TRIGGERKEY control is a free-text box that takes a bare letter (`k`) — nothing tells me whether that means Ctrl+K, Cmd+K or plain K, and nothing stops me typing something that collides with a browser shortcut. I wanted a modifier picker plus a conflict warning. The ITEMS JSON also lets me type `{"type":"navigate","to":"/anything"}` with no validation that the route exists in this project.
- **Status:** NEEDS_FIX

### Redirect
- **Bugs found:** Three, all reproduced in the browser.
  1. **A Redirect dropped with its default props makes the page permanently unreachable, and traps the Back button.** Repro: insert Redirect on `/nav-lab-6` (seeded `{"to":"/","label":""}`) → open the page in the production renderer (`http://localhost:6503/p/gh0mlpbp/nav-lab-6`). The URL immediately becomes `http://localhost:6503/` and the page reads **"404 This page could not be found."** Press Back → you land back on the page, it redirects again, and you end up on the same 404 (`history.length` stayed 3, `location.href` still `/`). Expected: I can still get out of a page I just previewed. Actual: the page is a one-way door. Severity: **blocker** — the same thing happens in the editor's built-in preview overlay and in a standalone tab, and there is no visible clue in the editor that a page has become unopenable.
  2. **`to` is resolved against the origin root and ignores the renderer's base path** — so *every* Redirect 404s in preview. Repro: set TO to `/nav-lab-6` (the page's own route) in the Props panel, reload `http://localhost:6503/p/gh0mlpbp/nav-lab-6`. The browser goes to `http://localhost:6503/nav-lab-6` — the `/p/gh0mlpbp` prefix is **dropped** — and shows 404. Expected: it resolves the same way the Breadcrumb/Link routes do, within the previewed app. Severity: **major**.
  3. **A self-referential Redirect does not loop, it soft-locks.** Repro: with TO = `/nav-lab-6` (its own route), the first paint shows the page with the text **"Redirecting…"** at the bottom and then bounces once to the 404 above. Where the base path *did* match, the page would sit on "Redirecting…" indefinitely. Good news for the brief's "does a Redirect loop?" question: **no infinite loop, no browser hang** — but the stuck "Redirecting…" state has no timeout and no escape. Severity: **minor** (relative to 1 and 2).
- **Missing/desired features:** the TO field is a free-text box; I wanted a picker listing the project's real routes (the editor already lists them in the Pages rail) and a guard that refuses `to` == the page's own route. There is also no design-time indication on the canvas that this node will make the page unopenable — it just renders the words "Redirecting…" like any other text. A "disabled while editing" toggle would have saved me from having to delete it to preview anything else on the page.
- **Status:** NEEDS_FIX

### Link
- **Bugs found:** **A freshly dropped Link is not a link — it goes nowhere, but looks exactly like one.** Repro: insert Link on `/nav-lab-6` → renders the text "Learn more" as blue, underlined, `cursor: pointer`. Open the page in the production renderer and click it: **nothing happens, the URL does not change.** The rendered anchor is `<a href="">` — an *empty* href (verified via the element's attributes in the live page), because the registry seeds `{"label":"Learn more","navigate":"","workflow":""}` and neither destination is set. In the editor canvas the anchor has **no `href` attribute at all**. Expected: either a seeded destination, or a visibly "unset" state. Actual: a control that is styled as a working link and is inert. Severity: **major**. This is round 5's C3c class (`""` seeded where absent was correct — CodeBlock.code / QRCode.value), in a navigation component where the consequence is a dead link rather than an empty box.
  **And setting a destination does not fix it.** Follow-up repro: select the Link in the editor, type `/items` into the NAVIGATE prop, blur. The editor updates correctly — the canvas anchor becomes `href="/items"` and so does the anchor in the production renderer. Click it in the production renderer (verified I hit the right element: `document.elementFromPoint` at the anchor's centre returns `A|Learn more`, `href="/items"`, `onClick` present on its React fiber). **The URL does not change.** So the Link's own click handler swallows the navigation even when a valid destination is configured — the empty seed is the visible symptom, but the component is inert either way. Severity: **major**, and this is the part to fix first.
- **Missing/desired features:** the destination is split across two free-text props (`navigate` and `workflow`) with no indication of which one wins if I fill both, and no route picker — the editor knows this project's routes (they are listed in the Pages rail) and does not offer them. I also wanted `target="_blank"` / external-URL support and an `aria-current`/disabled state.
- **Status:** NEEDS_FIX

### NavLink
- **Bugs found:** **Same as Link — a nav item that navigates nowhere, plus no active state.** Repro: insert NavLink → renders a padded pill reading "Link". In the production renderer the anchor is `href="#"` (registry seeds `{"label":"Link","target":"","icon":""}`). Click it: the URL does not change and no hash is added. It also carries **no `aria-current`** attribute at all, so even once you give it a target there is nothing marking the current page — which is the one thing a NavLink is for. Severity: **major** for the dead default, **minor** for the missing active state (untested with a real target because the default gives nothing to compare against).
- **Missing/desired features:** a route picker for `target` instead of free text; an `activeClassName` / active style control; and the `icon` prop is a bare text box with no icon picker even though the editor resolves icons elsewhere.
- **Status:** NEEDS_FIX

### SkipLink
- **Bugs found:** **The skip link reaches nothing.** Repro: open `/nav-lab-6` in the production renderer, click into the page, then Shift+Tab / Tab to the first focusable element. The link correctly becomes visible on real keyboard focus (147x24, `clip: auto`, `:focus-visible` matches) — that part works. Press Enter: **the URL hash stays empty, focus stays on the link, nothing scrolls or moves.** Cause is visible in the DOM: the component's default `target` is `"main"`, so it renders `href="#main"`, but the rendered page's `<main>` element has attributes `style, data-project-id, data-page-path, data-register` and **no `id`** — `document.getElementById('main')` is `null`. Severity: **major** (this is an accessibility component whose whole job silently fails).
  - *Retracted false positive:* calling `.focus()` programmatically leaves it 1x1 with `clip: rect(0,0,0,0)`, which looks like "invisible when focused". Driving a real Tab keypress shows it reveals correctly. Not a bug.
- **Missing/desired features:** the TARGET field is free text with no validation and no picker; I expected it to default to something that exists in the generated page shell, or for the renderer to stamp `id="main"` on its `<main>`. A user has no way to discover that the default is broken.
- **Status:** NEEDS_FIX

### ContextMenu — addendum, production renderer
- **Bugs found:** **In the real app it does not open at all.** Repro: open `http://localhost:6503/p/gh0mlpbp/nav-lab-6`, right-click the "Right-click here" target → **no `[role=menu]` appears anywhere in the document**; you get the browser's own context menu instead. So the behaviour is worse in the app than in the editor: the editor at least showed an empty 160x10 popup, the app shows nothing. The renderer says why — the server console logs `[scaffold] schema validation failed for gh0mlpbp/nav-lab-6 (3 issues), rendering raw. First: ["root.children.4.type: type is already covered by a strict node shape, or collides with a reserved structural bucket", ...]` and **children 4, 5 and 9 are exactly ContextMenu, DropdownMenu and Menubar** — the three nodes whose required content prop the registry does not seed. Same class as round 5's C3 (the editor writes page JSON its own schema rejects), new instances. Severity: **major**.
- **Missing/desired features:** as above — an `items` control.
- **Status:** NEEDS_FIX

### DropdownMenu — addendum, production renderer
- **Bugs found:** **Completely dead in the real app.** Repro: open the page in the production renderer, click the "Actions" button → `aria-expanded` stays `"false"`, `data-state` stays `"closed"`, no `[role=menu]` in the document. Focus it and press Enter → same. The button's React fiber does have `onPointerDown` / `onKeyDown` wired, so the handler exists and the open state simply never flips. This node is one of the three that fail the scaffold's schema validation (`root.children.5`, see the ContextMenu addendum). Severity: **major**.
- **Missing/desired features:** as above.
- **Status:** NEEDS_FIX

### Menubar
- **Bugs found:** **The Props panel for Menubar is completely empty, and the component renders an empty bar.** Repro: insert Menubar on `/nav-lab-6` → it renders a 960x24 `<div class="flex items-center gap…">` with **no text and no items**, and gets the editor's amber empty-node hint reading *"Menubar — Horizontal application menu bar."* — the palette description verbatim, naming no prop to fill in. Select it: the Properties panel shows **`Menubar` / `menubar-gs1inx` / the ALL·SM·MD·LG·XL breakpoint row and nothing else — zero controls.** The persisted node is literally `{"type":"Menubar","props":{}}`. In the production renderer it is the same empty 960x24 bar. It is also one of the three nodes the scaffold rejects: `[scaffold] schema validation failed … root.children.9.type`. Severity: **major**. This is round 5's `props: {}` class (rows 1–3, Carousel / Lightbox / Tree) — same class as round 5 row 1, new instance in the navigation category.
- **Missing/desired features:** everything — a `menus` control (top-level menu titles, each with items, separators, submenus and shortcuts). As it stands a user cannot put a single item in a Menubar from this editor.
- **Status:** NEEDS_FIX

### CartBadge — addendum, production renderer + selectability
- **Bugs found:** Confirmed in the real app: in `http://localhost:6503/p/gh0mlpbp/nav-lab-6` the CartBadge is the third child of the page container and renders as `SPAN[0x0] childElementCount=0` — **no DOM, no icon, no count, in the app as well as in the editor**, even though the registry does seed real props (`{"href":"/cart","label":"Cart","hideZero":false}` — note `hideZero` is *false*, so a zero count should still be shown). Follow-on: because the node has no box **and this editor has no layers/outline panel** (the left rail is Pages only), there is no way to select it — I could not open its Props panel, restyle it, or delete it once dropped. Severity: **major**, upgraded from the initial entry.
- **Missing/desired features:** as above, plus: a fallback/empty rendering so the node is at least selectable, or a layers panel so zero-box nodes are reachable.
- **Status:** NEEDS_FIX

### `/items` (scaffold preview — :6510 is down, see the standalone entry above)
- **Bugs found:** **The item list is invisible. The page shows its header and three stat tiles and nothing else.** Repro: open `http://localhost:6503/p/gh0mlpbp/items`. Visible body text is exactly: *"Inventory Items … Total Inventory Value 46851.48 · Low Stock Items 0 · Items 10"* and then it stops. The search box, the "All items / Low stock" filter tabs, the Table/Cards view toggle and the **entire 10-row table** are all present in the HTML but parked in React streaming buffers that are never swapped in — `div[hidden] id="S:0"` (empty), `S:1` = `All itemsLow stock`, `S:2` = `ViewTableCards`, `S:3` = `ItemsAll stocked items, newest first…` containing all 11 `<tr>`. `document.querySelectorAll('a').length === 0` on the whole page. Reproduced on a fresh navigation and on a hard reload. Expected: a browsable inventory list. Actual: a header, three numbers, and no way to do anything. Severity: **blocker**.
- **Root cause I could observe from the browser:** the page emits the Suspense-completion script (`$RC("B:1","S:1")` is in the document and `window.$RC` is a live function) but the content stays in the hidden div, and for some boundaries the completion script is never emitted at all. This same failure hits every route below **and** the Breadcrumb entry above — it is one bug, not five.
- **Missing/desired features:** even when the table is dug out of the hidden div, **the rows contain no links** — only `Edit` / `Delete` buttons — so there is no click-through from a row to `/items/[id]`. As a user wanting to open a record I had nothing to click; I had to type the URL.
- **Status:** NEEDS_FIX

### `/items/new` (scaffold preview)
- **Bugs found:** **The page is completely blank.** Repro: open `http://localhost:6503/p/gh0mlpbp/items/new`. `document.body.innerText` is the **empty string**; `<main>` measures **1525 x 0**. The whole form — Item Name, Category, Quantity, Unit Price and the "Add Item" button — exists in the DOM (`input[name=name]`, `category`, `quantity`, `price`) but every field measures 0x0 and `input.closest('div[hidden]')` is truthy: it is all inside `div[hidden] id="S:0"`. Severity: **blocker**.
- **NOT TESTABLE IN BROWSER — could not submit valid or invalid data, because there is no visible form to submit.** Everything the brief asked for here (empty submit, valid submit, navigate away mid-edit) is blocked behind the blank page.
- **Missing/desired features:** n/a until the page renders.
- **Status:** NEEDS_FIX

### `/items/[id]` (scaffold preview)
- **Bugs found:** Two, and the second is the worst thing I found today.
  1. **The `[id]` segment is ignored — every id shows the same record.** Repro: open `/p/gh0mlpbp/items/1` → "Wireless Bluetooth Headphones · Electronics", qty 42, price 89.99. Now open `/p/gh0mlpbp/items/2` → **the identical record**. Now open `/p/gh0mlpbp/items/99999`, an id that does not exist → **still the identical record**, no 404, no "not found", no error. So a deep link into a detail page does *not* load the right record, it loads the first one, and a bad id is indistinguishable from a good one. Severity: **blocker**.
  2. **Half the record is invisible.** The visible text is only `Wireless Bluetooth Headphones · Electronics · Stock status · Edit Item`. `Quantity in Stock 42`, `Unit Price 89.99`, `Line Value (Qty × Price)`, `Added 2024-…` and the **Delete Item** button are all stuck in `div[hidden] id="S:0"` / `id="S:1"` — the same streaming failure as `/items`. Severity: **blocker**.
  3. Minor, spotted inside the hidden block: **`Line Value (Qty × Price)` renders as `—`** although quantity is 42 and unit price is 89.99. Severity: **minor** (and it belongs to a display component outside this batch — flagging it, not claiming it).
- **Missing/desired features:** a real not-found state for an unknown id; and a "back to list" link — there is none.
- **Status:** NEEDS_FIX

### `/items/[id]/edit` (scaffold preview)
- **Bugs found:** Two.
  1. **The page is completely blank.** Repro: open `http://localhost:6503/p/gh0mlpbp/items/2/edit` → `document.body.innerText` is the empty string. The form (Name, Category, Quantity, Unit Price, "Save Changes", "Cancel") is in `div[hidden] id="S:0"` / `id="S:1"`. Severity: **blocker**.
  2. **The server-rendered edit form arrives with every field empty.** Reading the markup out of the hidden buffer, the four inputs are `name=""`, `category=""`, `quantity=""`, `price=""` (no `value` attribute at all) for a record whose real values are `Wireless Bluetooth Headphones / Electronics / 42 / 89.99`. Expected: an edit form prefilled with the record. Severity: **blocker** if it stands. **Caveat, stated honestly:** these inputs have **no React fiber** (`__reactProps$…` is absent) because they live in a Suspense buffer that was never adopted, so I could not tell whether a client-side effect *would* have filled them once the page hydrated. This one needs re-testing after bug 1 is fixed — do not treat it as confirmed.
- **NOT TESTABLE IN BROWSER — could not submit, cancel, or navigate away mid-edit,** because the form never becomes visible.
- **Missing/desired features:** n/a until the page renders and prefills.
- **Status:** NEEDS_FIX

<!-- Agent A — navigation batch, session notes.

ONE ROOT CAUSE ACCOUNTS FOR SIX OF THE ENTRIES ABOVE. On a full document load
the scaffold renderer never completes its React Suspense boundaries: the content
is streamed into `<div hidden id="S:n">` and the `$RC("B:n","S:n")` completion
script is simply not emitted (`window.$RC` exists; no call to it does). Anything
inside a boundary is therefore in the HTML and invisible. That is the whole of
the Breadcrumb bug, the whole of `/items/new` and `/items/[id]/edit` being blank
pages, and most of `/items` and `/items/[id]`. Forcing a client re-render (I did
it accidentally by clicking a link) makes the content appear correctly. Fix it
once in the renderer and six entries above collapse to two.

WHAT WORKS, verified and worth recording:
- The CommandPalette does NOT capture the editor's keyboard. Ctrl+K with focus
  in the editor's own "Search components…" box left the box focused and its
  value untouched; Ctrl+K on the canvas opened nothing. ContextMenu is likewise
  correctly scoped: right-clicking a *different* node opened no menu. The
  round-4/5 document-listener trap does not recur in this batch.
- The Style panel works on Breadcrumb: BACKGROUND = `color.primary.500` resolved
  to `rgb(59, 130, 246)` on the rendered `<nav>` (computed, not just persisted),
  and sizing landed on the wrapper. Round 5's "style is dropped or applied
  unresolved" class does not recur here.
- The JSON prop editor has a real error state: invalid JSON in Breadcrumb.ITEMS
  showed "Not saved — Expected double-quoted property name in JSON at position
  19" and kept the last good value rather than blanking the node.
- Browser Back/Forward behave correctly on the previewed app on every page that
  has no Redirect on it.
- Zero React warnings and zero console errors from any of the ten components,
  in the editor or in the renderer. The only editor console traffic was Fast
  Refresh (three fix agents were rebuilding under me throughout).

ENVIRONMENT, for whoever reads this next:
- Editor :6501 was fine but slow — the first compile of /editor/[projectId]
  exceeded 180s while the fix agents were rebuilding; after that, ~7s.
- Scaffold :6503 was up the whole session (first compile 106s).
- Standalone app :6510 was down the whole session (see its entry).
- Scratch page used: /nav-lab-6. /items, /ops-dashboard, /stock-intake,
  /input-lab-2, /input-lab-3, /display-lab-4 and /display-lab-5 were not edited.
  The Redirect node was deleted from /nav-lab-6 after testing so the page is
  previewable; everything else was left in place for Agent B to reproduce.
-->

---

#### CORRECTION (coordinator) — the standalone app and the "Suspense never completes" finding

Agent A's navigation batch reported two things that **do not reproduce** and that
fixers should NOT act on. Both were measured against a state that has since
changed, or misattributed. Recorded here so the final report is honest.

**1. `:6510` is NOT down.** The `Module not found: Can't resolve './FigmaCanvas'`
blocker was real, and I fixed it while Agent A was still running — the file was
missing from `backend/templates/app-foundation/src/lib/` entirely, so *every*
generated app failed to compile, not just this one. Written to the template
(root cause, all future generations) and copied into `output/gh0mlpbp/app`.
Re-measured after the fix:

| route | before | after |
|---|---|---|
| `/items` | 500 | **200** |
| `/items/new` | timeout | **200** |
| `/items/[id]` | timeout | **200** |
| `/items/[id]/edit` | timeout | **200** |

**2. "The scaffold renderer never completes its Suspense boundaries" — NOT
CONFIRMED.** Measured directly on `:6503/p/gh0mlpbp/items`: **4 hidden
`<div hidden id="S:n">` boundaries and 4 `$RC(` completion calls**, i.e. every
boundary completes, with 5300 visible characters of content. The claim that
`$RC` "is never emitted" does not hold against the current build.

**3. What the blank page actually was — auth, not rendering.** The generated app
served 31 characters (`Skip to main content I Loading…`) and appeared frozen.
Driven in a real browser it **redirects to `/login`** and renders a complete
sign-in page (heading "Welcome back", `email` + `password` inputs, "Sign in"
button, footer). The app is behind NextAuth and no session existed. Every route
returning a loading shell to an unauthenticated client is correct behaviour, not
a renderer bug.

**Consequence for the route entries.** `/items`, `/items/new`, `/items/[id]` and
`/items/[id]/edit` were audited **unauthenticated**, so "blank page", "list
invisible", "form arrives empty" and "`[id]` ignored — 1, 2 and 99999 render the
same record" are all unsafe conclusions: an unauthenticated client never reached
the page. **These four routes need re-auditing with a signed-in session before
any fix is attempted.** Agent A flagged the `[id]/edit` hydration caveat itself,
which was the right instinct.

Still open and NOT invalidated by this correction — these were observed in the
**editor canvas**, which needs no auth, and stand as logged: CartBadge rendering
zero DOM, Menubar's empty Props panel, DropdownMenu being unselectable,
CommandPalette's Ctrl+K, Link/NavLink inert defaults, SkipLink's `#main` target,
and Redirect's default making a page unopenable.

---

#### FIX — empty-node hint, rows 27/28 (`Stepper`, `Carousel`, `DescriptionList`, `List`, `Tree`, `ValidationChecklist`)  (frontend)
- **Fix applied:** the hint is now derived from the **component's own Zod schema**, not from the registry descriptor. `frontend/src/components/canvas/empty-hints.ts` gains `schemaMissingProp()`, which reads `propsSchema` off the live library registry (`buildDefaultRegistry().list()`, built lazily on first hint) and ranks the node's blank props structurally: (1) required *and* list-shaped, (2) required, (3) list-shaped with a `[]` default. Wrapper-peeling (`optional` / `default` / `catch` → not required; `nullable` / `preprocess` / `brand` / `pipeline` / `lazy` → still required) is purely by Zod type, so there is **no per-component table anywhere** — it is one rule over all 133 components and it follows a component when its schema changes. `hintFor()` consults it before the old registry-descriptor path, which stays as the fallback for registry-only entries. Row 27: Stepper now reads *“set “steps” in the Properties panel.”* (`steps` is the required array; the old hint named `bind`, `z.string().optional()`, the one prop that was never the problem). Row 28: the five that named no prop at all now name `items` / `images` — their schemas default to `[]`, so nothing was *required*, which is exactly why the descriptor-driven code fell through to the palette blurb.
  Where the schema requires a prop the Properties panel has **no control for**, the hint says so — `“<Type> — needs “<prop>”, which has no control yet.”` — rather than pointing at a control that does not exist. That case is a registry gap, routed below, not patched here.
- **Feature added:** a swept test rather than a fixture list — `frontend/src/__tests__/empty-node-hints.test.tsx` walks every leaf in `starterRegistry`, asks the schema what is missing, and asserts the hint names *that* prop. Assertions are on the prop NAME, never on "a hint appeared": the Stepper bug was a real hint on a real empty node, so a presence-only test passed against it.
- **Status:** RESOLVED

#### FIX — empty-node hint, row 29 (`Lightbox` 960×0, `Dialog` 0×0, `BulkActionBar`, `CartBadge`)  (frontend)
- **Fix applied:** `frontend/src/components/canvas/EmptyNodeHints.tsx` no longer discards a hint when the node's box is smaller than 24×12 — which was backwards, since the node that lays out at 960×0 or 0×0 is the one that most needs marking (invisible *and* undiagnosable). Any empty node smaller than `MIN_HINT_W`×`MIN_HINT_H` (120×16) is now padded out to that minimum and drawn at the node's own position, carrying `data-empty-hint-ghost` and a stronger amber so "renders nothing at all" reads differently from "renders an empty box". The rule is **geometric, not per-component** — nothing in the code knows what a Lightbox is. Editor-only: it inflates the overlay's rectangle, never the node's, so a shipped app's layout is untouched. The hint stays `pointer-events-none`, so a ghost box over a zero-height node's neighbours still cannot eat a click or a drop.
- **Feature added:** none requested. Side effect worth noting — `CartBadge` (span, 0×0, no DOM) now gets a visible marker on the canvas, so the "there is nothing on the canvas to click, select or diagnose" half of that entry is diagnosable; the *selectability* half is not fixed by this and is tracked separately.
- **Status:** RESOLVED

#### ROUTED — registry gaps found by the same sweep (not patched in the frontend)
Running the new schema-vs-descriptor check over the whole registry names, generically, every leaf whose component requires a prop the Properties panel offers no control for. This is the machine-checked version of the auditors' ContextMenu / DropdownMenu / Menubar findings, and it turned up four more:
- `ROUTED: registry — DropdownMenu needs "items" (z.array({label,value,icon?,disabled?})); no descriptor, panel shows only "trigger".`
- `ROUTED: registry — ContextMenu needs "items" (same shape); no descriptor, panel shows only "label".`
- `ROUTED: registry — Menubar needs "menus" (z.array({label, items:[{label,value}]})); entry has props:{} — the empty Props panel reported above.`
- `ROUTED: registry — MoneyInput needs "currencies".`
- `ROUTED: registry — Kanban needs "columnOrder".`
- `ROUTED: registry — CartPanel needs "paymentMethods".`
- `ROUTED: registry — CartPage needs "paymentMethods".`
Until those descriptors exist the canvas hint tells the user the prop is missing *and* that there is no control for it, instead of sending them hunting.
- **Status:** ROUTED

#### GATES (after the above)
`npx tsc --noEmit` → 49 errors, unchanged from the pre-existing baseline (all in `src/lib/*.test.ts` navFlow fixtures). `npx vitest run` → 47 files / **622** tests / 0 failures (613 baseline + 9 new). `npx vitest run src/__tests__/editor-validation.test.tsx` → 21/21, `selectable=129 invalidProps=0 unknownType=0`.

<!-- Agent A — feedback batch (21 components). Scratch page /feedback-lab-7 in
     project gh0mlpbp, own Chrome tab 2076428911. Contract half executed with
     `npx tsx` against the real Zod modules (component <Name>.schema.ts, the
     starter registry, and NodeV2/PageV2); browser half is javascript_tool DOM
     probes on the live canvas. className/style excluded from "missing" counts.
     NOTE: the editor's active page appears to be shared across browser tabs —
     one Alert node (`alert-brw9s7`) of mine landed on /data-lab-7 before I
     noticed and Delete/Backspace on the selected node would not remove it.
     Whoever owns /data-lab-7 should drop that node. -->

### Alert
- **Bugs found:** none. Repro: palette search "Alert" → insert on `/feedback-lab-7` → node `alert-fl8gmf`, **960x48**, renders `Alert message` in a real `role="alert"` box. Contract diff is exact: the Zod object has 4 props (`message`, `variant`, `title`, `style`), the registry exposes all 3 non-style ones, the `variant` enum matches the Zod enum value-for-value **and in order** (`neutral|info|success|danger|warning`), no dead props, no `null` defaults, no control mismatches. `defaultPropsFor("Alert")` = `{message:"Alert message",variant:"neutral",title:""}` parses clean against `AlertProps` **and** against `NodeV2`. `style` is destructured and passed through `resolveStyle`; `useMotion` is wired.
- **Missing/desired features:** the component branches on the `elevation` and `radiusScale` theme tokens but nothing in the Props panel says so — the same Alert looks different between two projects with no visible cause. Minor: no `dismissible` (its sibling `Banner` declares one), and no icon slot even though the palette description promises "severity variants" which usually implies an icon.
- **Status:** CLEAN

### Banner
- **Bugs found:** **`dismissible` is unreachable from the editor** — it is the only Banner prop with no registry descriptor, and it is the one that turns the ✕ button on. Repro: insert Banner → node `banner-u5tiuj`, 960x44, text `Message`; the Props panel offers exactly VARIANT / TITLE / MESSAGE, and there is no way to author a dismissible banner from the editor at all. Expected: a toggle. Actual: the prop exists in `Banner.schema.ts`, is destructured in `Banner.tsx:29` and gates the entire close button, and the editor cannot set it. Severity: **minor** (nothing breaks; a documented feature is simply not authorable).
- **Missing/desired features:** the four-value `variant` enum matches the schema exactly, but there is no `icon` and no `action`/link slot, so a "page-level notification banner" cannot carry the call-to-action that is the usual reason to show one. Also, `Banner` keeps its dismissed state in local `useState`, so in the editor a dismissed banner cannot be brought back except by re-selecting the node — worth a design-time override the way Dialog needs one.
- **Status:** NEEDS_FIX

### Spinner
- **Bugs found:** the dispatcher wrapper for the node measures **0x0 with `display: contents`** (`spinner-nolxmu`) while the actual spinner inside is 24x24 and visible. The component itself is fine — it renders `role="status"` with the sr-only label — but the *selectable box* the canvas draws for the node has no area, so the node is hard to click and any hint overlay has nothing to attach to. This is the mild form of round 5's C9/#29 (Lightbox/Dialog), not the fatal one: the spinner IS visible. Severity: **minor**.
  Contract half is otherwise clean: 4 Zod props, registry exposes `label` and `size`, the `size` enum matches `sm|md|lg` exactly, defaults parse against both `SpinnerProps` and `NodeV2`.
- **Missing/desired features:** no `variant` (dots/bars) and no colour control — the ring is hardwired to `border-t-primary`, so a spinner on a primary-coloured surface is invisible and the Style panel cannot fix it (`resolveStyle` reaches the outer span, not the ring). A user who puts a Spinner on a dark Hero gets nothing.
- **Status:** NEEDS_FIX

### LoadingState
- **Bugs found:** none. Insert → `loadingstate-75f50y`, **960x132**, renders `Loading…`. The Zod object is `.strict()` with exactly `label` (`z.string().min(1)`) and `style`; the registry seeds `label: "Loading…"`, which is a real non-empty value, so the `.min(1)` is satisfied — the failure mode that killed `CodeBlock.code` and `QRCode.value` in round 5 does not occur here. Parses clean against `LoadingStateProps` and `NodeV2`.
- **Missing/desired features:** it is a one-prop component — no `variant` (spinner vs. skeleton vs. text), no `size`, no way to say "this is loading a table" versus "loading a card", so every loading state in a generated app looks identical. Given that `Skeleton` and `Spinner` both exist, `LoadingState` having no way to choose between them is the gap a user notices first.
- **Status:** CLEAN

### EmptyState
- **Bugs found:** none. Insert → `emptystate-tvtnye`, **960x124**, renders `Nothing here yet.` plus a `Get started` button. All 3 non-style Zod props are exposed; `action` is correctly typed `type:"object", control:"json"` against the `union({label,workflow}, {label,navigate})` schema and seeded with a valid `{"label":"Get started","workflow":"createRecord"}` — this is the shape round 4/5 got wrong on eight other components (`actionPicker` on an object prop) and it is right here. Defaults parse clean against `EmptyStateProps` and `NodeV2`.
- **Missing/desired features:** `action` is a raw JSON textarea, so a user has to know that the two legal shapes are `{label,workflow}` and `{label,navigate}` — nothing in the panel says so, and the `description` on the descriptor is the only hint. A two-field control (label + a picker that offers this project's workflows and routes) is what a user expects. Also no `title` — the whole state is one line of message text.
- **Status:** CLEAN

### EmptyStateRich
- **Bugs found:** `illustration` carries **`default: null`** in the registry (`starter.ts`, `emptyStateRichEntry`). `normalizeSeed` now strips `null` before it reaches the node, so this no longer breaks validation — but it is still the wrong way to spell "no seed", and it is the only `default: null` in this whole batch. Per the brief, logging it, **not** treating it as a P0. Severity: **minor**.
  Everything else measured clean: insert → `emptystaterich-hckvbm`, **960x274**, renders the 📦 glyph, `Nothing here yet`, a `Get started` CTA and a `Load sample data` link. `primaryCta` and `sampleDataLink` are both `type:"object", control:"json"` with structurally valid seeds. Defaults parse against `EmptyStateRichProps` and `NodeV2`.
- **Missing/desired features:** `illustration` accepts `union(string, {slug,alt,tone})` and resolves against `/p/<projectId>/illustrations`, but the control is a bare JSON box — there is no picker showing which illustration slugs this project actually has, so the one prop that distinguishes `EmptyStateRich` from `EmptyState` is authorable only by someone who already knows the slug list.
- **Status:** NEEDS_FIX

<!-- Agent A — data batch (batch 3). Scratch page /data-lab-7 in project gh0mlpbp,
     own Chrome tab. Phase 1 = contract diff (registry ↔ component Zod ↔ PageV2,
     introspected with tsx against the real schemas). Phase 2 = live DOM probes
     on the editor canvas. -->

### Sparkline
- **Bugs found:** **A Sparkline dropped from the palette is invisible — it draws a line with `stroke: none`.** Repro: palette search "Sparkline" → click to insert on `/data-lab-7`. The node lands (`sparkline-aq8mkf`, wrapper 960x24, a real `<svg>` 100x24 with a `<polyline>` whose `points` are correct: `0.0,16.7 20.0,10.4 40.0,13.6 60.0,4.1 80.0,7.3 100.0,1.0`). But the polyline's `stroke` **attribute is the empty string** and its computed `stroke` is **`none`** — SVG treats an invalid paint value as the initial value, so nothing is painted. The geometry is right and the ink is off. Counterfactual proven in the same session: select the node → Props → set COLOR to `#ff0000` → the attribute becomes `#ff0000`, computed `rgb(255, 0, 0)`, and the line appears. Cause: the registry seeds `color: { type:"string", default:"", control:"color" }` (`starter.ts` sparklineEntry) while `Sparkline.tsx` declares `color = "currentColor"` as a **JS parameter default**, which only fires for `undefined`. `""` is not `undefined`, so the seed overrides the component's own fallback. This is the `ActivityFeed.maxHeight: 0` class exactly — a registry default read as a command rather than as "unset" — and the round-5 C3c class (`""` seeded where absent was correct). Severity: **major** (drop it and there is nothing to see; the node's box is 24px tall so it is barely selectable, and nothing tells you why).
  Second, smaller: **the COLOR control lies about the current value.** It renders as a native `<input type="color">` whose value reads `#000000` while the prop is actually `""`. So the panel says "black", the canvas says "none", and because a native colour input cannot express "no value" there is **no way to get back to unset** once you touch it.
  Third: the DATA textarea's placeholder is `[{"value": "one", "label": "Option one"}]` — the `Select.options` placeholder — even though this prop is `z.array(z.number()).min(2)`. And the helper line under it reads *"Editing as JSON — rows are not all the same shape"* for a plain array of numbers. Both mislead about the shape required (severity: minor).
  Contract check (tsx introspection): registry ↔ `SparklineNode.shape.props` agree on all five props, no dead props, no truncated enums, and the registry defaults **pass** both the component schema and `PageV2` on a fresh drop.
- **Missing/desired features:** nothing missing from the editor — all five schema props are exposed. What I wanted was a token picker for COLOR (the Style panel offers `Primary · 500` etc. for backgrounds; the sparkline stroke gets a raw hex swatch only) and a way to clear the colour back to "inherit from text".
- **Status:** NEEDS_FIX

#### FIX — DropdownMenu (and every node that acts on pointer-down)  (frontend)
- **Fix applied:** the canvas now takes selection on **pointer-down in the capture phase**, ahead of any runtime handler on the node. `frontend/src/components/canvas/hooks/useSelection.ts` gains `useCanvasPointerDown()` (sharing the modifier logic with the existing click path), wired in `Canvas.tsx` as `onPointerDownCapture` / `onMouseDownCapture` on the canvas root.
  Root cause, and why this is not a DropdownMenu fix: overlay primitives open on **pointer-down**, and while open they put `pointer-events: none` on the document body. By the time the `click` the canvas used to select on was dispatched, the element under the cursor was no longer the node — `closest("[data-node-id]")` returned null and the canvas *cleared* the selection instead of making it. Every component that opens on pointer-down had the same fate; nothing in the fix knows what a DropdownMenu is. `stopPropagation()` on the captured synthetic event is what makes the canvas inert — React replays propagation itself, so the target's own `onPointerDown` never runs and the runtime menu no longer opens while the user is editing. This is the "usual design-time convention of suppressing runtime click handlers inside the canvas" the entry asked for, one event layer below `INERT_NAVIGATOR`.
  Deliberately **not** `preventDefault()`: the defaults there are focus and the start of a native drag, and the canvas needs both (reorder is HTML5 drag). Primary button only, so a right-click still reaches the browser; pointer-down on bare canvas is left alone so a background drag is unaffected.
- **Feature added:** none requested. `frontend/src/__tests__/canvas-selection-pointerdown.test.tsx` (6 tests) covers the report's exact sequence — select on pointer-down, runtime handler suppressed, and selection surviving a follow-up click that has lost the node.
- **Status:** RESOLVED

#### GATES (after the pointer-down fix)
`npx tsc --noEmit` → 49, baseline unchanged. `npx vitest run` → 48 files / **628** tests / 0 failures.

---

<!-- Fixer R — registry pass 1. All edits confined to packages/registry/src/starter.ts
     and packages/registry/tests/. Gates run after every batch: registry build
     (134 entries), packages/registry vitest (27/27), frontend
     editor-validation.test.tsx (21/21, invalidProps=0, selectable=129), and a
     throwaway PageV2 sweep that drops all 133 palette components and parses the
     resulting page. That sweep went 9 failing -> 7, and the remaining 7 are all
     schema-side and routed below. -->

#### FIX — DropdownMenu  (registry)
- **Fix applied:** added the `items` descriptor the component exists for — `type: "array"`, `control: "json"`, seeded `[{label:"Edit",value:"edit"},{label:"Duplicate",value:"duplicate"},{label:"Delete",value:"delete"}]`. `json`, not `actionPicker`: an item is `{label, value, icon?, disabled?}` (DropdownMenuNode/MenuItem is `.strict()` on exactly those four) and actionPicker's only output is an action object, which validateProps' step-3 coercion replaces with `[]`. Seeded rather than empty for the same reason `Breadcrumb.items` is: an empty `json` textarea tells the user nothing about the shape, and a menu that opens empty on drop is indistinguishable from a broken one. Checked against BOTH contracts before writing: `DropdownMenuProps` (library) and `DropdownMenuNode` (schema).
- **Feature added:** `triggerIcon` (iconPicker) and `align` (select: start/center/end) — both declared on DropdownMenuNode and reachable from nothing. NEITHER carries a default: they are `.optional()`, so a seed would freeze a choice on every dropped menu, and `""` on an optional string is present-and-meaningless. Row-level `disabled` is documented in the `items` description (it is part of MenuItem), which covers the auditor's ask for a disabled row; a genuinely *destructive* row style is a component change — see the route below.
- **Status:** RESOLVED
- **Not fixed here (routed):** the node was unselectable on the canvas because the trigger swallows the click — that is design-time click suppression in the editor canvas. ROUTED: frontend — trigger-bearing nodes need runtime click handlers suppressed inside the canvas, or a layers panel. The dead-in-the-app open state and the empty-menu affordance are library-side and already in flight there (MenuEmptyNote).

#### FIX — ContextMenu  (registry)
- **Fix applied:** added the same `items` descriptor (same shape, same seed). LABEL was the only control the panel offered, so the menu a ContextMenu exists to show could not be authored at all — the persisted node was `{"type":"ContextMenu","props":{"label":"Right-click here"}}`.
- **Feature added:** none beyond `items` — `ContextMenuNode` declares only `label` + `items`, so there is nothing further the registry can honestly expose.
- **Status:** RESOLVED

#### FIX — Menubar  (registry)
- **Fix applied:** `props: {}` — the entry had ZERO controls — now carries `menus`: `type: "array"`, `control: "json"`, seeded `[{label:"File",items:[{label:"New",value:"new"},{label:"Open",value:"open"}]},{label:"Edit",items:[...]}]`. Shape is MenubarNode's exactly: `MenubarItem` is `.strict()` on `{label, value}` — no icon, no disabled — so the seed and the description say that and no more.
- **Feature added:** the auditor asked for separators, submenus and shortcuts. NOT added, deliberately: `Menubar.tsx` renders `RMenubar.Item` only and `MenubarItem` is `.strict()` on two keys, so a registry control for them would write props nothing reads and the schema rejects — the exact defect this round is fixing. ROUTED: library/schema — Menubar needs separator / submenu / shortcut support on MenubarItem before the editor can offer it.
- **Status:** RESOLVED

#### FIX — Link  (registry)
- **Fix applied:** removed the `""` defaults from `navigate` and `workflow`. `Link.tsx` puts `navigate` straight into `href`, so the seeded `""` rendered `<a href="">` on every dropped Link — blue, underlined, `cursor:pointer`, and completely inert. Absent lets `LinkProps.navigate`'s own `.default("#")` apply, which is the component's declared "no destination yet" value. `workflow` is `.optional()` and gated on `if (workflow)`, so `""` was only a longer spelling of absent that made the prop look set in the panel.
- **Feature added:** none requested that the registry can supply. `target="_blank"` / external-URL handling, an explicit unset/disabled visual state and `aria-current` are all props `LinkProps` does not have and `Link.tsx` does not read. ROUTED: library — Link needs target/rel, a visibly-unset state, and aria-current. ROUTED: frontend — destination props (Link.navigate, NavLink.navigate, Redirect.to, SkipLink.target) want a picker over the project's real routes instead of free text.
- **Status:** RESOLVED

#### FIX — NavLink  (registry)
- **Fix applied:** the entry's two behaviour props were **dead descriptors** — `NavLink.tsx` reads `href | navigate | label | children | currentPath | className | style` and neither `target` nor `icon` is among them, so `target` was a text box whose value validateProps silently stripped and `icon` was an icon picker with no reader at all. Replaced `target` with `navigate` (the name the component and the `unifyLabelHref` remap actually read) and removed `icon`. `navigate` carries NO default for the same reason as Link's — `""` would put `href=""` on the anchor; absent lets NavLink.tsx's own `?? "#"` apply. The missing `aria-current` the auditor reported is not missing from the component (`NavLink.tsx` emits it when `currentPath === dest`); it was unreachable because the destination prop the user filled in was being thrown away.
- **Feature added:** none. The icon control is not re-added as a lie. ROUTED: library — NavLink needs an `icon` prop and an activeClassName before the registry can expose them.
- **Status:** RESOLVED

#### FIX — Redirect  (registry)
- **Fix applied:** removed the `to: "/"` default. This is the command-default trap in its purest form: `Redirect.tsx` runs `nav.replace(to)` in a mount effect, so a seeded `to` is not a placeholder value, it is an instruction the component executes the instant the node exists — which is why a Redirect dropped with its defaults made the page permanently unopenable and trapped the Back button. The mount effect is gated on `if (to)`, so an unseeded Redirect sits quietly showing "Redirecting…" until the user names a destination. Also dropped the `label: ""` seed (`.optional()`, rendered as `label || "Redirecting…"`).
- **Feature added:** none requested that the registry can supply. The base-path bug (`to` resolved against the origin root, so every Redirect 404s under `/p/<project>/`), the self-route guard and the "disabled while editing" toggle are all outside the registry. ROUTED: library/renderer — Redirect.to must resolve through the Navigator's base path, and refuse a `to` equal to the page's own route.
- **Status:** RESOLVED

#### FIX — Grid  (registry) — class fix, not logged by an auditor
- **Fix applied:** removed `rowGap`, `columnGap`, `padding` and `align`. They were dead in both directions at once: `renderer/src/nodes/layout/Grid.tsx` reads exactly `columns`, `gap`, `rows`, `equalRows`, `equalCols`, `className`, `style` — the four had no reader, so setting them moved nothing on the canvas — and `V2GridNode.props` is `.strict()` over `{columns, rows, gap, equalRows, equalCols}`, so seeding them ALSO made every palette-dropped Grid produce a page `PageV2` rejects. That is the same mechanism as the ContextMenu addendum's `[scaffold] schema validation failed … rendering raw`, where three bad nodes took the other nine on the page down with them. `gap` remains and covers the common case.
- **Feature added:** none. ROUTED: renderer/schema — Grid should implement rowGap / columnGap / padding / align (unread by Grid.tsx today, and undeclared on V2GridNode) before the editor offers them again.
- **Status:** RESOLVED

#### FIX — Skeleton  (registry) — class fix, not logged by an auditor
- **Fix applied:** removed the `lines: 3` default. `variant` defaults to `"rect"` and `SkeletonNode` carries a cross-field refinement — "Skeleton.lines is only valid when variant is 'text'" — so the two seeds contradicted each other and every dropped Skeleton produced a page `PageV2` rejects. The control stays; its description now says it is only valid when VARIANT is text.
- **Feature added:** none requested.
- **Status:** RESOLVED

#### FIX — the 21 input `name` seeds  (registry) — class fix, 21 descriptors, one rule
Input, Textarea, Select, Checkbox, Switch, NumberInput, RadioGroup, Slider, FileUpload, Combobox, TimePicker, ColorPicker, InputOTP, Rating, MaskedInput, KeyValueInput, SegmentedControl, Calendar, RichTextEditor, CameraCapture, DatePicker.
- **Fix applied:** removed `default: ""` from all 21 `name` descriptors. `name` is `z.string().min(1)` on every input's node schema, so `""` is not "unset" — it is present-and-too-short, and it made the registry's own published seed the one value the schema is guaranteed to reject. It was never what a dropped field actually carried either: `buildDroppedNode` derives `name` from the node id (`input_a1b2c3`) so two "Email" fields do not collide. Unseeded, the drop path behaves exactly as before and the registry stops publishing an invalid value to its other consumers (the JSON export, the LLM catalog, the properties panel).
- **Feature added:** none requested.
- **Status:** RESOLVED

#### FIX — Repeat / Conditional  (registry) — class fix, not logged by an auditor
- **Fix applied:** removed the `""` seeds from `Repeat.source`, `Repeat.bind` and `Conditional.when`. `V2RepeatNode.props.source` is `z.string().min(1).optional()` (absent is valid, `""` never is); `V2ConditionalNode.props.when` is an Expression that rejects the empty string outright; and `bind` is not a declared key of `V2RepeatNode`'s `.strict()` props at all, so seeding it guaranteed a rejected page. All three render identically when absent, so nothing on the canvas changes.
- **Feature added:** none requested. `bind` is kept as a control because `renderer/src/nodes/data/Repeat.tsx` genuinely reads `props.bind`. ROUTED: schema — V2RepeatNode.props needs `bind` (the runtime reads it; the node documents it as a TOP-LEVEL key the properties panel cannot reach).
- **Status:** RESOLVED

#### FIX — registry ↔ node-schema parity guard  (registry) — the mechanism, not the instances
- **Fix applied:** new `packages/registry/tests/node-schema-parity.test.ts`. Every defect above is one class: *the registry publishes a prop or a value the component's own `.strict()` node schema refuses, the editor writes page JSON `PageV2` rejects, and the scaffold drops the WHOLE page out of validated rendering.* The test introspects `NodeV2`'s own union (the same one `PageV2` validates with, resolved after a warm-up parse so the `z.lazy` body is built) and asserts four things per prop, with no page assembly and no copy of the frontend drop pipeline: that the strict shapes actually resolved (guard-the-guard, so it can never pass vacuously); that no registry prop is absent from the strict node props shape; that no `default` is a value that prop's schema refuses; and that every prop the node marks REQUIRED has a control to fill it — which is exactly what Menubar / ContextMenu / DropdownMenu failed. Numeric-domain enums (`Heading.level: "2"`) are exempted the same way `normalizeSeed` exempts them, keyed off the descriptor and never off a component name. The outstanding schema-side gaps are listed by name with their reason, so they have to be deleted rather than the list quietly grown.
- **Feature added:** n/a (test).
- **Status:** RESOLVED

#### NOT FIXED by the registry — routed, with reasons (nothing left silent)
- **CartBadge** — `CartBadge.tsx` returns `null` whenever `count == null`, which is every editor canvas and every app without a live `/api/cart`. The registry defaults are already correct and complete (`href:"/cart"`, `label:"Cart"`, `hideZero:false`); no descriptor change can make a component that returns `null` render, and seeding a fake count would be a command default. ROUTED: library — CartBadge must render a design-time/zero state instead of null. (Already in flight there.)
- **SkipLink** — the registry default `target: "main"` matches `SkipLinkProps`' own `.default("main")` and its documented contract ("the id the shell template stamps on its `<main>`"). The failure is that the rendered `<main>` carries no `id`. Changing the registry default would point the link at a different element that also does not exist. ROUTED: renderer/scaffold — the page shell's `<main>` must carry `id="main"`.
- **Breadcrumb** — no registry defect: `items` is already a seeded `json` array with the right shape and `separator` is correct. The ask is a row-per-crumb editor with a route picker and a `currentPageAuto` option. The first two are a control the editor does not have; `currentPageAuto` is not a prop `BreadcrumbProps` or `BreadcrumbNode` accepts, so a descriptor for it would be a control writing a prop nothing reads. ROUTED: frontend — array-of-objects props want a row editor with a route picker (Breadcrumb.items, CommandPalette.items, DropdownMenu/ContextMenu.items, Menubar.menus). ROUTED: library/schema — Breadcrumb needs currentPageAuto.
- **CommandPalette** — registry seeds are correct (two real sample commands, placeholder, triggerKey). `triggerKey` is left as free text because `CommandPaletteNode` types it as a plain string: an enum of modifiers would be a registry control writing a shape the schema does not accept. ROUTED: frontend — triggerKey wants a modifier picker plus a browser-shortcut conflict warning. ROUTED: library — the advertised Ctrl+K listener never fires, focus is not moved into the search box on open, and the overlay has no `role="dialog"` / `aria-modal`.
- **`/items`, `/items/new`, `/items/[id]`, `/items/[id]/edit`, and the standalone app on :6510** — no registry surface at all. The React-streaming boundaries that never swap in, the ignored `[id]` segment, the unprefilled edit form and `Can't resolve './FigmaCanvas'` are generator/scaffold defects. ROUTED: scaffold/generator — not a registry change. Flagged explicitly rather than left silent.
- **Container** — the six flex props (`direction`, `gap`, `padding`, `align`, `justify`, `wrap`) are fully implemented by `renderer/src/nodes/layout/Container.tsx` (its header documents them) but `V2ContainerNode.props` is `.strict()` over `maxWidth` alone, so **every palette-dropped Container makes its page fail `PageV2`**. Deliberately NOT patched by deleting working controls or unseeding them — that would change the default look of every new page in order to fix a schema bug. ROUTED: schema — V2ContainerNode.props needs direction/gap/padding/align/justify/wrap. This is the widest instance of the class, since Container is the node every page is built inside.
- **FileUpload / RadioGroup / TimePicker** — same shape: `filenameField`, `mimeTypeField`, `resumable`, `retryOn5xx`, `chunkSizeMb` (declared in `FileUpload.schema.ts`) and `disabled` (destructured and forwarded by `RadioGroup.tsx` / `TimePicker.tsx`) are all implemented and all rejected by their `.strict()` node schemas. ROUTED: schema — FileUploadNode needs the five Wave-3 upload props; RadioGroupNode and TimePickerNode need `disabled`.
- **Tabs** — `tabs: []` is refused by `TabsNodePlain.props.tabs.min(1)`, but a freshly dropped Tabs legitimately has no panels and Tabs now derives one tab per CHILD. Seeding a tab would cap the strip at one panel — the exact bug the Tabs remap exists to undo. ROUTED: schema — TabsNode.props.tabs should allow `[]` (under-configured is not malformed), the same relaxation the menu nodes just received.
- **Repeat / Conditional, structural** — both are `children.min(1)` on their node schemas, so an empty control-flow node dropped from the palette can never be valid no matter what the registry seeds. ROUTED: schema — V2RepeatNode / V2ConditionalNode children should allow `[]` while unconfigured.

### IllustratedEmpty
- **Bugs found:** **The registry seeds the required title as the empty string, so the component drops onto the canvas with no words on it.** Repro: palette search "IllustratedEmpty" → insert on `/feedback-lab-7`. The node (`illustratedempty-df4qvp`, then `illustratedempty-osb989`) measures **960x239** and its `innerText` is **`""`** — the SVG glyph renders and the heading is blank. The persisted page JSON is literally `{"kind":"list","title":"","message":"","action":""}`. `IllustratedEmptyProps.title` is `z.string().min(1)` and **not** optional; `safeParse(defaultPropsFor("IllustratedEmpty"))` fails with `title: too_small`. Expected: a seeded headline the way `EmptyState` seeds `"Nothing here yet."`. Actual: a 240px-tall illustration with an empty heading where the headline belongs. Severity: **major**.
- **Second bug in the same entry:** **`action` has the wrong descriptor type entirely.** The Zod prop is `union({label,workflow}, {label,navigate})` — an object — and the registry declares it `type:"string", control:"text", default:""`. The Props panel therefore offers a plain text box for a structured action, and the seed `""` makes the same props parse fail a second time with `action: invalid_union`. Because `validateProps` step-3 coercion has no branch for `too_small` or `invalid_union`, **both errors fall through and the raw props are handed to the component uncoerced** — so every Zod `.default()` on this component is skipped, which is round 5's C2 mechanism arriving through a different door. `""` is falsy so the CTA silently renders nothing rather than crashing. Severity: **major**.
- **Missing/desired features:** the ten-value `kind` enum is complete and correctly ordered, which is the one thing this entry gets right — but with the title blank, none of the ten presets is usable out of the box. A user also expects the CTA to be authored with a label field plus a workflow/route picker, exactly as `EmptyState.action` already is in the sibling entry.
- **Status:** NEEDS_FIX

### Skeleton
- **Bugs found:** **A palette-dropped Skeleton writes page JSON that the project's own `PageV2` schema rejects.** Repro: insert Skeleton → the node is seeded `{"variant":"rect","lines":3}` (both registry defaults, verified via `defaultPropsFor`). `NodeV2`'s `superRefine` carries the invariant *"Skeleton.lines is only valid when variant is 'text'"*, so `NodeV2.safeParse({id,type:"Skeleton",props:{variant:"rect",lines:3}})` **FAILS** at `props.lines`. This is the **only one of the 21 that fails the node-schema check**, and it fails on nothing but its own untouched registry defaults. Expected: the seed the editor writes is valid against the schema the editor validates against. Actual: `rect` + `lines: 3` is a contradiction the registry ships by default. Severity: **major** (a page that cannot be re-validated is a page no strict consumer can re-open).
  The render itself is fine — `skeleton-xs9v9y` measures 960x64 and shows the `animate-pulse bg-muted` block, because `Skeleton.tsx` only reads `lines` inside the `variant === "text"` branch. So the invalid prop is *silent* at render time and only bites at validation time, which is the worst of both.
- **Missing/desired features:** `lines` is offered unconditionally, with no indication that it applies to exactly one of the three variants — the Props panel should hide or disable it for `rect`/`circle` rather than seeding a value the page schema forbids. There is also no `width`/`height` control, so a `rect` skeleton is always `h-16 w-full` and cannot be shaped to stand in for the thing it is loading.
- **Status:** NEEDS_FIX

### Progress
- **Bugs found:** **`showValue` is unreachable, so a Progress can never display its own number.** Repro: insert Progress → `progress-wd1wy3`, 960x58, renders the label `Progress` and a filled bar; the Props panel offers exactly LABEL / VALUE / VARIANT. `ProgressProps` declares 8 props; `max`, `showValue` and `bind` have **no registry descriptor at all**. `showValue` is the flag that gates the percentage readout in both the bar and the circular branch (`Progress.tsx`), so from the editor the percentage is unauthorable. Switching `variant` to `circular` makes it worse: that branch renders a ring **and nothing else** — no label, no number — so a circular Progress in a generated app is a decorative arc with no readable value. Severity: **major**.
  `max` being unreachable means every Progress is hardwired to a 0–100 scale; a "3 of 7 steps complete" progress cannot be expressed. `bind` being unreachable means the value cannot be driven from data at all — a progress bar that can only ever show the literal number typed at design time. Severity: **major**.
- **Missing/desired features:** no colour control, no indeterminate state (the obvious companion to `Spinner`), and no size. Given `Gauge` exists separately with threshold zones, `Progress` at least needs its own value readout.
- **Status:** NEEDS_FIX

### PresenceIndicator
- **Bugs found:** **Renders absolutely nothing on the canvas, and gets no empty-node hint either.** Repro: palette search "PresenceIndicator" → insert on `/feedback-lab-7` → node `presenceindicator-ufyu7g` exists in the tree but measures **0x0**, `childElementCount === 0`, `display: contents`, `innerText` empty. There is no DOM under it whatsoever. I probed the canvas for the hint overlay and there are **zero** hint elements on the page — the zero-sized box has nothing for the overlay to attach to. This is round 5's Lightbox/Dialog failure and the navigation batch's CartBadge failure, a third time: **invisible AND undiagnosable**. Severity: **major**.
  Cause (explains the observation, does not stand in for it): `PresenceIndicator.tsx` ends its setup with `if (users.length === 0) return null;`. `users` is populated only from a `users` prop — which is **not in the Zod schema and not in the registry**, so nothing can ever set it — or from `window.__forgePresenceHook__`, which the renderer installs at runtime and the editor canvas does not. So in the editor the list is permanently empty and the component permanently returns `null`.
- **Missing/desired features:** a design-time preview. Every other avatar-ish component seeds sample data; this one needs either a seeded `users` array (which would also make `max`, `size` and `showTooltips` meaningful, since all three are exposed and none can currently change anything you can see) or a canvas-only placeholder showing two or three stub avatars. As shipped, the four exposed props are four controls that provably cannot alter the render.
- **Status:** NEEDS_FIX

### UndoManager
- **Bugs found:** **Renders absolutely nothing on the canvas, with no hint.** Repro: insert UndoManager → node `undomanager-qbrypp`, **0x0, `kids: 0`, `display: contents`**, empty text, and no hint overlay anywhere on the page. Identical shape to PresenceIndicator above. Severity: **major**. Cause: `UndoManager.tsx` returns `null` while `entries.length === 0`, and entries arrive **only** from a `window` `CustomEvent("forge:undo:push")` dispatched by the renderer's mutation queue — which never fires in the editor. All four exposed props (`position`, `timeoutMs`, `labelPrefix`, `maxStack`) therefore control something the author can never see.
- **Second bug:** **`position` is a free-text box against a four-value Zod enum.** The registry declares `position: { type:"string", control:"text", default:"bottom-center" }`, but `UndoManagerProps.position` is `z.enum(["bottom-left","bottom-center","bottom-right","top-center"])`. The panel invites the user to type, and any typo — `bottom-centre`, `bottomCenter` — fails the props parse; `validateProps` has no coercion branch for `invalid_enum_value`, so the raw string is passed through, `POSITION_STYLES[position]` is `undefined`, and the toast stack lands unpositioned in the top-left corner of the viewport. Same class as round 5's `SplitArc.segments`, one type over. It should be `type:"enum", control:"select"` with the four options. Severity: **major**.
- **Missing/desired features:** a design-time "show a sample toast" toggle, the way `InspectorPanel` gained `defaultOpen` for exactly this reason. Without one, `position` and `maxStack` cannot be checked against the page layout they will eventually cover.
- **Status:** NEEDS_FIX

---

<!-- Fixer R — registry pass 2. Picks up the feedback/data batch entries and the
     seven `ROUTED: registry — …` lines from the frontend fixer's schema-vs-
     descriptor sweep. Same gates: registry build (134 entries), registry vitest
     27/27, frontend editor-validation 21/21 invalidProps=0, plus
     drop-null-defaults and empty-node-hints (40 tests green together), plus the
     throwaway PageV2 drop sweep (still 7 failing, all schema-side, all routed). -->

#### FIX — Sparkline  (registry)
- **Fix applied:** removed the `color: ""` default. `Sparkline.tsx` declares `color = "currentColor"` as a JS **parameter** default, and a parameter default only fires for `undefined` — `""` is a value, so the registry seed beat the component's own fallback, the polyline got `stroke=""`, and SVG resolves an invalid paint to `none`. The geometry was right and the ink was off, which is why the node measured 100x24 with correct `points` and drew nothing. Same class as `ActivityFeed.maxHeight: 0` (a seed the component reads as a command rather than as "unset"), and it was the only `""` left on a `control: "color"` descriptor in the whole registry — checked, not assumed.
- **Feature added:** none. The auditor's other two Sparkline findings are not registry-side: the native `<input type="color">` reporting `#000000` for an unset prop and offering no way back to unset, and the JSON textarea showing the `Select.options` placeholder for a `z.array(z.number())` prop, are both control-rendering bugs. `ROUTED: frontend — the color control must distinguish "unset" from black and offer a clear-to-inherit; the json control's placeholder/helper text must come from the descriptor's own shape, not a fixed Select example.` The token picker for stroke colour is the same control gap.
- **Status:** RESOLVED

#### FIX — Banner  (registry)
- **Fix applied:** added the `dismissible` toggle. It was the only Banner prop with no descriptor and the one that turns the ✕ on: `BannerProps.dismissible` is declared, `Banner.tsx:29` destructures it and it gates the entire close button, and the editor could not set it — so a dismissible banner was not authorable at all. Seeded `false`, which is a safe seed and not a command: it is exactly what the component already does when the prop is absent, and `BannerNode` types it `z.boolean().optional()`.
- **Feature added:** none beyond that. `icon` and an action/CTA slot are not props `BannerProps` or `BannerNode` have, so a control for them would write props nothing reads. `ROUTED: library/schema — Banner needs an icon and an action/link slot.` The dismissed-state-stuck-in-local-useState problem is a design-time override, `ROUTED: library/frontend — Banner (like Dialog) needs its dismissed state overridable while editing.`
- **Status:** RESOLVED

#### FIX — EmptyStateRich  (registry)
- **Fix applied:** `illustration` no longer spells "no seed" as `default: null`; the key is simply omitted. `default?: unknown` on `PropDescriptor` already means absent, unambiguously and with nothing for a downstream reader to strip. The decision NOT to seed is unchanged and its reason is kept in the comment (a `{ slug }` resolves to `<basePath>/<slug>.svg` and no illustration assets ship with the editor, so any seed puts a broken `<img>` on every dropped node).
- **Feature added:** none. The illustration-slug picker the auditor wanted needs the editor to know which slugs a project actually bundles. `ROUTED: frontend — EmptyStateRich.illustration wants a picker over the project's /illustrations assets rather than a bare JSON box.`
- **Status:** RESOLVED
- **Deliberately NOT swept:** the other 50 `default: null` entries stay. On binding/action descriptors `null` is a *documented convention with two readers* — `normalizeSeed` (frontend) and `validateProps` step 3 (library) both strip it — and it carries information absence does not: "deliberately unseeded" versus "nobody got round to it". Churning 50 descriptors to remove a marker that two mechanisms already honour would be cosmetic, and it would delete that distinction. The one fixed above was the outlier: a `json`/object descriptor, outside the convention's documented scope, where `null` was only a second spelling of absent.

#### FIX — MoneyInput  (registry) — `ROUTED: registry — MoneyInput needs "currencies"`
- **Fix applied:** added `currencies` (`type: "array"`, `control: "json"`). It is the list the currency dropdown offers, and it had no control at all — so turning CURRENCYEDITABLE on gave the user a picker they could not populate. NO default: `Money.tsx:78` falls back to `DEFAULT_CURRENCIES` (nine codes) whenever the prop is absent or empty, so any seed here would silently NARROW the picker on every dropped field — a seed read as a restriction rather than as "unset".
- **Feature added:** n/a (this was the feature).
- **Status:** RESOLVED

#### FIX — Kanban  (registry) — `ROUTED: registry — Kanban needs "columnOrder"`
- **Fix applied:** added `columnOrder` (`type: "array"`, `control: "json"`), which `KanbanNode.props` already declares. NO default, and for a sharper reason than usual: `columnOrder` is an explicit column ALLOW-LIST as well as an ordering, so any seed would hide every lane it did not name. Absent means "derive the lanes from the data", which is what a board without it should do.
- **Feature added:** n/a.
- **Status:** RESOLVED

#### FIX — CartPanel + CartPage  (registry) — `ROUTED: registry — CartPanel/CartPage need "paymentMethods"`
- **Fix applied:** added `paymentMethods` (`type: "array"`, `control: "json"`) to both. NO default on either: it is `z.array(z.string()).optional()` and a seeded list would advertise payment methods the app may not actually accept — a seed read as a claim rather than as "unset".
- **Feature added:** n/a.
- **Status:** RESOLVED

#### NOT FIXED by the registry — pass 2 routes, with reasons
- **Alert** — audited CLEAN and the contract diff is exact. The asks (a `dismissible` like Banner's, an icon slot, and surfacing that the component branches on the `elevation`/`radiusScale` theme tokens) are all props `AlertProps` does not have; a descriptor for any of them would be a control writing something nothing reads. `ROUTED: library/schema — Alert needs dismissible + an icon slot`, and `ROUTED: frontend — the Properties panel should say when a component's look is driven by theme tokens rather than by its props.`
- **Spinner** — the 0x0 `display: contents` dispatcher wrapper is a canvas/selection concern, not a descriptor: both Spinner props are exposed and the `size` enum matches the schema exactly. `ROUTED: frontend — a display:contents wrapper gives the node no selectable box.` `ROUTED: library — Spinner needs a colour/variant control; the ring is hardwired to border-t-primary, so a spinner on a primary surface is invisible and resolveStyle cannot reach it.`
- **LoadingState** — audited CLEAN; it is genuinely a one-prop component. `ROUTED: library — LoadingState needs a variant (spinner | skeleton | text) and a size; with Skeleton and Spinner both existing, having no way to choose between them is the gap a user hits first.`
- **EmptyState** — audited CLEAN, and `action` is already correctly typed `object`/`json` against the two-shape union. The ask is a two-field control with a workflow/route picker, which is the same control gap as the array-of-objects editors. `ROUTED: frontend — object/union props want a structured control that names their legal shapes.`
- **`default: null` sweep** — see the EmptyStateRich entry above for why the remaining 50 are left as they are.

---

<!-- Fixer B (library / schema / renderer). Edits confined to
     packages/library/src, packages/schema/src, packages/renderer/src and the
     matching tests/ — plus ONE file outside that list, flagged explicitly under
     "Navigator base path" below. Every claim marked "verified live" was measured
     headlessly against :6503 / :6510 with Playwright, not inferred. -->

#### FIX — CartBadge  (library)
- **Fix applied:** `packages/library/src/components/CartBadge/CartBadge.tsx`. The component returned `null` whenever the count was not a number — which is every render before the first fetch resolves, every 401, every network error, and every render on a surface with no `/api/cart` at all (the editor canvas above all). That is why the node had `childElementCount === 0`, a 0x0 rect, no hint overlay, and nothing to select. An unknown count now renders as a dimmed `0` carrying `data-cart-count-known="false"`, which is also the honest thing to show a signed-out visitor. `hideZero` now hides only a count we actually fetched and that really is zero, so it can no longer make a freshly-dropped badge vanish. Fixed a rules-of-hooks violation in passing: `useMotion` was called *below* the `return null` guards, so the hook count changed with the fetch result.
- **Feature added:** none beyond the visible zero-state the entry asked for. Verified live on `:6503/p/gh0mlpbp/nav-lab-6` — the badge is now **101x32**, renders `Cart 0`, `href="/cart"`. Tests: `packages/library/tests/components/CartBadge.test.tsx` (5).
- **Status:** RESOLVED

#### FIX — ContextMenu · DropdownMenu · Menubar, empty menus  (library + schema)
- **Fix applied:** two changes, one shared each.
  1. **The empty menu says so.** `packages/library/src/util/MenuEmptyNote.tsx` — one inert row rendered by all three menus behind the same `length === 0` test (`ContextMenu.tsx`, `DropdownMenu.tsx`, `Menubar.tsx`, the last for both "no menus" and "a menu with no items"). The 160x10 white sliver with `childElementCount === 0` is gone. Deliberately a `div role="presentation"` and not a `Menu.Item`: keyboard-arrowing onto "No menu items" and pressing Enter must not fire a selection for a row that is not one. Shared rather than copied — three copies of six lines is how these three drifted apart in the first place.
  2. **An unconfigured menu no longer invalidates its whole page.** `packages/schema/src/nodes/overlay.ts`: `DropdownMenuNode.items`, `ContextMenuNode.items`, `MenubarNode.menus` and `MenubarMenu.items` were `z.array(...).min(1)`. `NodeV2` tries the strict shapes first and falls back to `anyRegistered`, which *deliberately refuses* any type a strict shape already covers — so a menu with no items matched nothing, `PageV2` failed, and the scaffold logged `schema validation failed … rendering raw`, dropping the **entire page** out of validated rendering. That is precisely the `[scaffold] schema validation failed for gh0mlpbp/nav-lab-6 (3 issues)` in the ContextMenu/DropdownMenu addenda: three unconfigured menus took the other nine nodes on the page down with them. "You have not filled this in yet" must not be spelled the same way as "this page is corrupt". Now `.default([])`, which the components render legibly. `backend/contracts/blueprint.schema.json` re-emitted (`npm run emit:blueprint-schema`).
- **Feature added:** none requested here. The `items` / `menus` **controls** were routed to registry and Fixer R has since landed them. Verified live: opening the "Actions" DropdownMenu on `:6503/p/gh0mlpbp/nav-lab-6` now yields a **160x42** menu reading "No menu items" instead of a 160x10 blank; the Menubar bar reads "No menus". Tests: `packages/library/tests/components/menu-empty-state.test.tsx` (5).
- **Status:** RESOLVED
- **Not fixed here:** Menubar separators / submenus / keyboard shortcuts (Fixer R's route to library). Not done in this pass — it needs `MenubarItem` widened in `packages/schema` and a matching registry descriptor, i.e. a coordinated three-package change; logging it as outstanding rather than half-landing it.

#### FIX — Redirect  (library)
- **Fix applied:** `packages/library/src/components/Redirect/Redirect.tsx` now refuses two redirects it used to perform.
  1. **No destination.** An unset `to` fell through `if (to)` in silence and left the page showing the word "Redirecting…" forever, which reads as a hang. It now renders `Redirect — no destination set.` and never navigates.
  2. **A redirect to the route it is already on.** Bug 3 in the entry — the soft-lock with no timeout and no escape. Refused at the source; the page renders normally with `Redirect skipped — "<to>" is this page.` This belongs in the component, not the host: every renderer that mounts a Redirect has the same hazard.
  The self-route test is a **trailing whole-path suffix**, not string equality, so it still fires under a preview base path (`/nav-lab-6` vs `/p/gh0mlpbp/nav-lab-6`) — which is exactly where it does the most damage — and does not false-positive on `/line-items` vs `/items`. The current path is read *inside* the effect rather than via state, so the redirect cannot fire on the first commit before the guard it is gated by has a value.
  Bug 1 (the seeded `to: "/"` making a page permanently unopenable) was the registry default acting as a command; routed to registry and Fixer R has removed it.
- **Feature added:** the guard the entry asked for ("a guard that refuses `to` == the page's own route") is the fix above. The route **picker** is a Properties-panel control — `ROUTED: frontend — Redirect.to / Link.navigate / NavLink.navigate / SkipLink.target want a picker over the project's real routes` (Fixer R has routed the same thing). Tests: `packages/library/tests/components/Redirect.test.tsx` (9).
- **Status:** RESOLVED

#### FIX — Navigator base path — Redirect bug 2, and the same line under Link, NavLink, Button navigate, Table rowHref and post-submit redirect  (renderer)
- **Fix applied:** this was never a Redirect bug. Every schema-driven navigation goes through one seam, `useNavigator()`, and with no provider mounted it falls back to `window.location.assign(url)` — which resolves `/items` against the **origin root**. Measured before the fix: clicking a Link with `navigate="/items"` on `/p/gh0mlpbp/nav-lab-6` landed on `localhost:6503/items`. Six components shared the one line, so the fix is in the seam: `packages/renderer/src/client/Navigator.tsx` gains `resolveWithBasePath()` and `createBasePathNavigator(basePath, inner?)`, exported from `packages/renderer/src/index.ts` and re-exported from `packages/engine/src/index.ts`. Absolute URLs, protocol-relative URLs, other schemes, bare fragments/queries, relative paths and anything already under the base are left untouched; only an app-absolute route is prefixed, and never twice.
  **One file outside my ownership, flagged deliberately:** the mechanism is inert until a host declares its prefix, and leaving it unwired would be a workaround that leaves the real cause standing. `apps/render-scaffold/src/components/SchemaRendererWrapper.tsx` now mounts `NavigatorProvider` with `createBasePathNavigator('/<projectId>', <next-router-backed navigator>)`. The inner navigator is `useRouter()`, not `window.location`, because Next applies its own `basePath` (`/p`, from `NEXT_BASE_PATH`) to router pushes and *not* to `location.assign` — so the framework owns its prefix and this adds only the project segment, correct whether or not `NEXT_BASE_PATH` is set. Soft navigation comes free. That file belongs to neither Fixer F (`frontend/src/**`) nor Fixer R; reassign if that is wrong.
- **Feature added:** none. Verified live: the same Link click now lands on `/p/gh0mlpbp/items` and renders the Items page. Tests: `packages/renderer/tests/base-path-navigator.test.ts` (8).
- **Status:** RESOLVED

#### FIX — Link  (library)
- **Fix applied:** `packages/library/src/components/Link/Link.tsx`.
  - **A link with nowhere to go is no longer rendered as a link.** `href={navigate}` with an empty destination produced `<a href="">` — focusable, blue, underlined, `cursor: pointer`, and it reloads the current page when clicked. `href=""` is *worse* than no href, because an anchor without `href` is not a link to assistive technology and does not invite the click in the first place. With neither `navigate` nor `workflow` set, the anchor now drops its `href`, its link colouring and its pointer cursor, and carries `data-link-unset` + `aria-disabled`. A `workflow`-only link stays fully interactive — only "no destination and no behaviour at all" is marked unset.
  - `LinkProps.navigate` default changed from `"#"` to `""`. **This matters for Fixer R's Link entry**, which removed the registry seed on the assumption that absent falls back to the component's `.default("#")`: `"#"` is not "unset", it is a real destination (jump to top) and it renders a live-looking link that does nothing. `""` now routes into the unset state above.
  - The navigation itself was not swallowed — that was the base-path bug above, now fixed; the entry's "setting a destination does not fix it" half is resolved by the Navigator change, verified live.
- **Feature added:** `target: "_self" | "_blank"` (`Link.schema.ts` + component). `_blank` also emits `rel="noopener noreferrer"` and is left to the browser rather than soft-navigated, which is the entry's `target="_blank"` ask. External-URL support already worked and is now covered by a test. `aria-current` is NavLink's job, not a plain Link's, and is done there. `ROUTED: registry — Link needs a "target" descriptor (select: _self/_blank) now that the component reads it.` Tests: `packages/library/tests/components/link-navigation.test.tsx`.
- **Status:** RESOLVED

#### FIX — NavLink  (library)
- **Fix applied:** `packages/library/src/components/NavLink/NavLink.tsx` + `NavLink.schema.ts`.
  - **`target` and `icon` are no longer dead.** The registry declared both and the component read neither, so every palette-dropped NavLink rendered `href="#"` however the user filled the panel in, and the icon picker had no reader. `target` is now accepted as a third alias for the destination alongside `href`/`navigate`, and `icon` is rendered through the shared icon registry. This is compatible with Fixer R's fix (which renamed the descriptor to `navigate`) — all three names now resolve — and it means the `icon` control Fixer R removed as "a lie" can be re-added truthfully: `ROUTED: registry — NavLink.icon is now read by the component; the iconPicker descriptor can come back.`
  - **`aria-current` now actually appears.** It was emitted only when `currentPath === dest`, and `currentPath` defaulted to `""` and was supplied by nothing except the generated app's own shell — so in the preview renderer, and in any host that just drops a NavLink on a page, no link was ever marked current. The component now reads the browser's path when it is not told one (in an effect, so SSR markup and first client paint agree), and matches by trailing whole-path suffix so `/items` is current at `/p/<project>/items` too. An explicit `currentPath` still wins.
  - **No destination → not a link**, same reasoning and same shape as Link: `data-navlink-unset`, no `href`, instead of a live-looking `href="#"`.
- **Feature added:** the icon rendering and the working active state are the entry's asks. `activeClassName` not added — `data-active="true"` and `aria-current="page"` are now on the element, which a stylesheet or a `className` can target without a new prop; say the word if a literal `activeClassName` is wanted. Verified live: the NavLink on `nav-lab-6` now renders with **no href and `data-navlink-unset`** instead of a fake `href="#"`. Tests: in `link-navigation.test.tsx`.
- **Status:** RESOLVED

#### FIX — SkipLink  (library)
- **Fix applied:** `packages/library/src/components/SkipLink/SkipLink.tsx` gains `resolveLandmark()`. The id was being treated as a contract when it is only a hint: `target` defaults to `"main"` because the shell template stamps `id="main"`, but nothing enforces it, and the hosts that do not stamp it are exactly the ones nobody checks. Measured on the preview renderer, `<main>` carried `style`, `data-project-id`, `data-page-path`, `data-register` and no id, so `getElementById("main")` was null and pressing Enter moved neither focus nor scroll — an accessibility component failing silently, which is the worst failure mode there is, because nobody who needs it is in a position to notice. The handler now falls back to the `<main>` / `[role=main]` landmark the id stood in for, and **stamps the id onto it**, so the anchor's own `href="#main"` and the browser's native fragment behaviour start working too rather than only this component's click handler. Only the conventional ids (`main`, `main-content`, `content`) fall back; a link pointing at some other id is never silently retargeted.
- **Feature added:** none requested beyond making the link work. Verified live on `:6503/p/gh0mlpbp/nav-lab-6`: activating the skip link now sets `main#main[tabindex="-1"]` and moves `document.activeElement` to `MAIN`. The auditor's own retraction (the link *does* reveal on real keyboard focus) is confirmed — no change needed there. Tests: in `link-navigation.test.tsx`.
- **Status:** RESOLVED

#### VERIFIED — `/items` · `/items/new` · `/items/[id]` · `/items/[id]/edit` on the standalone app (:6510)
- **Fix applied:** none by me — the coordinator's `FigmaCanvas.tsx` fix is the fix. Re-measured independently, twice: `curl` gives **200** on all four routes with no `Module not found` anywhere in the payload, and a headless browser confirms each one **redirects to `/login` and renders a complete sign-in page** (heading "Welcome back", email + password inputs, Sign in button) with **zero console errors and zero page errors**. That is a working app behind NextAuth, not a broken one. Agrees with the coordinator's correction.
- **Feature added:** none. The four scaffold-preview route entries are **not re-audited here** — per the coordinator's correction they were measured unauthenticated and need re-auditing with a session before anything is fixed.
- **Status:** RESOLVED (standalone-app entry only)

#### NOT REPRODUCED — "the scaffold renderer never completes its Suspense boundaries"
Independently re-measured on `:6503/p/gh0mlpbp/items` in a clean headless browser: **one** `div[hidden]`, and it is empty; `window.$RC` live with 4 completion scripts; 1217 characters of visible body text including the filter tabs and the Table/Cards toggle the entry reports as stuck. Nothing is parked in a buffer. Agrees with the coordinator's correction — no fix attempted, and the six entries that hang off this root cause should be re-audited rather than fixed.

#### GATES (Fixer B, pass 1)
`packages/schema` build clean + `emit:blueprint-schema` re-run. `packages/patches`, `packages/renderer`, `packages/library`, `packages/engine`, `packages/registry` all build with 0 TS errors; registry export writes 134 entries / 158 contracts. `packages/registry` vitest **27/27**. `packages/renderer` vitest 256 passed / 30 failed — **unchanged pre-existing baseline** (`validation-errors.test.ts` expects `Page` parse to throw where the renderer now warns and renders as-is; unrelated to anything here), and the 8 new base-path tests pass. `packages/library` vitest: 1091 passing (baseline 1057 + 34 new).

### Tooltip
- **Bugs found:** **`side` is unreachable — a Tooltip can only ever open above its trigger.** Repro: insert Tooltip on `/overlay-lab-8` → node `tooltip-m0g7pc`; the Props panel offers exactly LABEL / CONTENT. `TooltipProps` declares `side: z.enum(["top","right","bottom","left"])` and `Tooltip.tsx` passes it straight to `RTooltip.Content`, but there is **no registry descriptor for `side` at all**, so the component's own `side = "top"` parameter default is the only value any authored Tooltip can ever have. `TooltipNode.props.side` also declares the enum, so this is a prop the node schema, the component schema and the component all know about and only the editor is missing. A tooltip on a control at the top of a page will always try to open off-screen. Severity: **major** (unauthorable behaviour, and the failure is invisible until the page is used).
- **Second bug (minor):** `className` is declared in `TooltipProps` and **never destructured** in `Tooltip.tsx` — the component reads `{ label, content, side, style, children }`. Same in Popover, HoverCard and Drawer (all four floating surfaces). Nothing in the editor writes it today, so this is latent rather than live, but the schema advertises a prop the component provably drops.
- **Renders / children / focus (the batch's key questions):** renders **fine** — the dispatcher wrapper `tooltip-m0g7pc` is the usual 0x0 `display: contents` span, but its child trigger is a real 73x24 box reading "Hover me", so the node is visible and the empty-node hint is correctly not needed. Hovering it with a real pointer at (801,178) opened a Radix popper containing **"Hint text"**, measured live — so unlike Dialog, the Tooltip's floating surface **does** work on the editor canvas with no provider. `slots: "leaf"`, so no child can be dropped in and nothing can be swallowed. It does not touch the editor's keyboard: the only focus affordance is `tabIndex={0}` on its own trigger.
- **Registry defaults vs the node schema:** `{label:"Hover me", content:"Hint text"}` → `NodeV2.safeParse` **PASS**, `TooltipProps.safeParse` **PASS**.
- **Missing/desired features:** besides `side`, there is no delay control (the component hardwires `delayDuration={0}`, so every Tooltip in a generated app fires instantly — no way to author the conventional ~700ms hover intent), and the trigger's dotted underline is hardwired. The registry default `label: "Hover me"` is **byte-identical to HoverCard's**, so two different components dropped side by side are indistinguishable on the canvas until you hover them — worth differentiating.
- **Status:** NEEDS_FIX

### HoverCard
- **Bugs found:** **The card's own placement and open/close timing are entirely unauthorable, and unlike its three siblings it has no side/align prop at all.** Repro: insert HoverCard → `hovercard-3afv29`, trigger box 74x24 reading "Hover me"; hovering it at (875,178) opens a real 256x46 popper reading "Details" (measured live). So it renders and it works. But `HoverCardProps` is only `{label,title,content,className,style}` — there is no `side` (Tooltip has one), no `align` (Popover has one), and `HoverCard.tsx` hardwires `openDelay={0} closeDelay={0}` and `sideOffset={6}`. A rich preview card that pops instantly on the slightest mouse-over is the one behaviour a hover card must not have, and there is no prop, in any of the three layers, to change it. Severity: **major** as a gap, not as a defect — nothing is broken, the component is simply under-specified relative to Tooltip and Popover.
- **Second bug (minor):** the registry seeds `title: ""`. `HoverCardNode.props.title` is `z.string().optional()` so `""` validates, but `HoverCard.tsx` gates the title row on `{title && ...}` — so the seed writes a key to disk that means exactly the same as omitting it. Every dropped HoverCard persists a prop that does nothing. Same shape as Popover's `title: ""`.
- **Third bug (minor):** `className` declared in `HoverCardProps`, never destructured in `HoverCard.tsx`.
- **Renders / children / focus:** renders visibly (the 0x0 node box is the shared `display: contents` dispatcher wrapper, its child trigger is real). `slots: "leaf"` — no children accepted, nothing swallowed. No editor keyboard capture; the trigger carries its own `tabIndex={0}` and nothing else.
- **Registry defaults vs the node schema:** `{label:"Hover me", title:"", content:"Details"}` → `NodeV2.safeParse` **PASS**, `HoverCardProps.safeParse` **PASS**.
- **Missing/desired features:** open/close delay, side/align, and — the one a user reaches for first — the ability to put *content* rather than a single string in the card. `HoverCard.tsx` does accept `children`, but the registry declares the component a leaf, so the rich preview a HoverCard exists for can only ever be one line of plain text. Also: default label collides with Tooltip's, see the Tooltip entry.
- **Status:** NEEDS_FIX

### Popover
- **Bugs found:** **`align` is unreachable — a Popover can only ever open centred on its trigger.** Repro: insert Popover on `/overlay-lab-8` → `popover-u0looj`; the Props panel offers exactly TRIGGER / TITLE / CONTENT. `PopoverProps` and `PopoverNode.props` both declare `align: z.enum(["start","center","end"])`, `Popover.tsx` passes it straight to `RPopover.Content`, and the registry has **no descriptor for it**, so `align = "center"` (the component's parameter default) is the only value any authored Popover can hold. A popover hung off a control at the edge of a page cannot be pulled back inside the layout. Severity: **major**. Same shape as Tooltip's missing `side`.
- **Second bug (minor):** the registry seeds `title: ""`, which `Popover.tsx` gates on `{title && ...}` — so every dropped Popover persists `"title":""` (confirmed in `output/gh0mlpbp/src/schemas/overlay-lab-8.json`), a key that means precisely the same as omitting it.
- **Third bug (minor):** `className` declared in `PopoverProps`, never destructured in `Popover.tsx`.
- **Renders / children / focus — this is NOT the Dialog failure:** the node is the usual 0x0 `display: contents` dispatcher wrapper, but its child trigger button is a real 61x34 box reading "Open", so the node is visible and selectable. **The floating panel does open on the editor canvas** — I dispatched a full pointerdown/up/click sequence on `[data-popover]` and read back `data-state="open"` with a live Radix popper containing "Content", *and* the node became selected in the same gesture (Properties panel populated). So `useCanvasPointerDown`'s `stopPropagation()` does not, in fact, make the Popover inert; the author gets both selection and a preview. `slots: "leaf"`, so nothing can be dropped in and nothing is swallowed. No editor keyboard capture.
  (Correction to my own first pass: a `computer left_click` at the trigger's screen coordinates left `data-state="closed"`. That was the automation not reaching the page — a click on a plain Heading in the same tab also failed to select it — not the component. Re-measured with dispatched events, which React and Radix both receive, and it opens.)
- **Registry defaults vs the node schema:** `{trigger:"Open", title:"", content:"Content"}` → `NodeV2.safeParse` **PASS** (`PopoverNode` requires `trigger`/`content` `.min(1)`, both satisfied), `PopoverProps.safeParse` **PASS**.
- **Missing/desired features:** `align`, a `side` (Popover has no side prop at all in any layer, so it can only ever open below), and — the big one — rich content. `Popover.tsx` renders `children ?? content`, i.e. it is *written* to host a subtree, but the registry declares it a leaf, so the panel a Popover exists to hold is limited to one string of plain text. Compare Drawer, which has the same code and the same limitation.
- **Status:** NEEDS_FIX

### Drawer
- **Bugs found:** **Opening a Drawer on the canvas puts a modal scrim over the entire editor, not over the page being edited.** Repro: insert Drawer → `drawer-xzqm7z`, trigger renders as a real 61x34 "Open" button; dispatch a click on `[data-drawer]` → `data-state="open"` and, measured live: a `div.fixed.inset-0.z-40.bg-black/40` covering **1525x678 — the whole browser viewport** — with the drawer panel at x=1205,y=0, 320x678, and `document.body { pointer-events: none }`. `document.elementFromPoint()` over the component palette returns the scrim, so the palette, the pages list and the Properties panel are all behind a modal layer belonging to a node on the canvas. The canvas itself occupies x=545..1823, so the drawer does not even land on the edge of the page it belongs to — it is pinned to the browser window. Expected: a design-time preview inside the canvas frame, the way `Dialog` now renders inline when it has no provider. Actual: the editor is modal-blocked by the artefact being edited. Severity: **major**. Recoverable — Escape closes it (verified: `data-state` back to `closed`, scrim count 0, `body` pointer-events back to `auto`) and the ✕ inside the panel is visible — so not a blocker, but the user has to discover that the way out of their own editor is Escape.
- **Second bug:** **`description` is unreachable.** `DrawerProps` declares it, `DrawerNode.props` declares it, and `Drawer.tsx` renders it as `RDialog.Description` — the accessible description Radix warns about when it is absent. The registry has **no descriptor**, so every authored Drawer ships without one. Severity: **major** (it is an a11y-required slot that the editor cannot fill).
- **Third bug (minor):** `className` declared in `DrawerProps`, never destructured in `Drawer.tsx`.
- **Renders / children / focus:** renders visibly (0x0 `display: contents` dispatcher wrapper around a real trigger button); `slots: "leaf"` so no child can be dropped and nothing is swallowed. No focus capture until opened; once open, Radix's own dialog focus containment applies — which is correct for a dialog and is the reason it swallows the editor while open.
- **Registry defaults vs the node schema:** `{trigger:"Open", title:"Panel", side:"right", content:"Content"}` → `NodeV2.safeParse` **PASS**, `DrawerProps.safeParse` **PASS**.
- **Missing/desired features:** `description`; a `size`/`width` (the panel is hardwired `w-80` for left/right and full-bleed for top/bottom, so a wide filter drawer is not authorable); an editor-only "show open" toggle, exactly the `defaultOpen` that InspectorPanel already has, so the title/description/content can be composed without modal-blocking the editor; and children — `Drawer.tsx` renders `children ?? content` but the registry declares it a leaf, so a drawer's body can only be one plain string.
- **Status:** NEEDS_FIX

### InspectorPanel
- **Bugs found (P0-class, the Dialog failure still live):** **With the registry's own untouched defaults an InspectorPanel renders nothing, gets no hint, and silently swallows anything dropped into it.** Repro on `/overlay-lab-8`: insert InspectorPanel → node `inspectorpanel-dl62nq` exists, measures **0x0**, `display: contents`, **`childElementCount === 0`**, no text. Then insert TourOverlay while it is selected. The saved page on disk (`output/gh0mlpbp/src/schemas/overlay-lab-8.json`) reads `InspectorPanel { … } → children: [ TourOverlay ]` — **the child persisted** — and there is **no `[data-node-id]` element for it anywhere in the canvas DOM**. Dropped work that is written to disk and never drawn: precisely the Dialog P0. Severity: **blocker**.
  I probed the whole document for the empty-node overlay: exactly **one** hint existed on the page and its text was `"FocusTrap — empty. Drag a component in here."` — nothing for InspectorPanel. It cannot get one: `EmptyNodeHints` resolves a node's box through `display: contents` wrappers by walking to the first child that generates one (`resolveBoxEl`, `empty-hints.ts`), and this wrapper has no children at all, so there is nothing to measure or draw over. Invisible **and** undiagnosable on the canvas. (The one mitigation: the node *does* appear in the editor's layer list, so it is at least selectable there.)
- **The cause is a registry default, not a missing mechanism.** `InspectorPanel.tsx` already carries the design-time fallback — `defaultOpen` exists, and its own docstring says it exists *"because the panel is otherwise unreachable from the editor: it renders `null` until `?<paramKey>=` is in the URL, which the canvas and the preview never set, so it was one of the components that render an EMPTY box with default props and swallow anything dropped into them"*. `InspectorPanelNode.props.defaultOpen` declares it. And then `inspectorPanelEntry` seeds it **`default: false`** — with a description that states the consequence out loud (*"Off, the panel renders nothing until the URL carries its param — which is why it is invisible on the canvas"*). The fix was built, documented, and then shipped switched off, so every palette-dropped InspectorPanel starts in exactly the state the fix was written to eliminate. Severity: **blocker**; it is also a one-character change.
- **Second bug (minor):** the registry seeds `title: ""`, which `InspectorPanel.tsx` reads as `{title || (active ? … : "Details")}` — the empty string is falsy so the fallback wins, and the key on disk means nothing.
- **Registry defaults vs the node schema:** `{paramKey:"inspector", title:"", width:"default", defaultOpen:false}` → `NodeV2.safeParse` **PASS** (`InspectorPanelNode` is not `.strict()` and every prop is optional or defaulted), `InspectorPanelProps.safeParse` **PASS**. The schema layer is fine; nothing here would have caught it.
- **Contract diff:** all four props exposed, no dead props, `width` enum complete (narrow/default/wide). No `className`/`style` in the node schema or the component at all, so unlike the four floating surfaces there is no dead style slot.
- **Missing/desired features:** `defaultOpen` seeded **true** (it is a design-time affordance; a runtime `?inspector=` still wins, per the component). Beyond that: a `side` — the panel is hardwired `end-0`, so a left-hand inspector is not authorable — and, given the panel is `position: fixed` and `aria-modal`, the same editor-scoping problem Drawer has: when it *is* open it covers the browser window rather than the canvas frame.
- **Status:** NEEDS_FIX

### FocusTrap
- **Bugs found:** **A FocusTrap dropped on the canvas takes the editor's keyboard focus on mount and then eats Tab.** Repro: load `/overlay-lab-8` in a fresh tab, click the page in the pages list, and — before touching anything else — read `document.activeElement`: it is a `DIV` with `data-forge-focus-trap="active" tabindex="-1"` sitting **inside `[data-canvas-root]`**, i.e. the FocusTrap node's own root. I then inserted five more components from the palette and re-read `document.activeElement` after each: it was the same trap root every time. With focus there, dispatching a `Tab` keydown returns **`defaultPrevented === true`** — the component's `onKeyDown` finds no focusables inside, calls `preventDefault()` and re-focuses the root. So the editor's Tab key is dead while a FocusTrap is on the page. Severity: **major** — this is the fourth component found stealing the editor's keys, and the same fix applies (the trap must not arm at design time, or must scope itself outside `[data-canvas-root]`).
  Cause (explains the observation): `FocusTrap.tsx`'s mount effect runs `autoFocus` unconditionally; with no focusable descendant it stamps `tabindex="-1"` on its own root and focuses that. `restoreFocus` makes it worse in the other direction — on every unmount it yanks focus back to whatever was focused when it mounted, which in the editor is a palette item.
- **Second bug:** **the trap is an invisible 960x0 strip.** The node wrapper is the usual 0x0 `display: contents` span; its real child div measures **960x0** because `FocusTrap.tsx` renders a bare `<div>` with no padding, no min-height and no border. It does at least get the empty-node hint — I read `"FocusTrap — empty. Drag a component in here."` off the overlay — so unlike InspectorPanel it is invisible but **diagnosable**. Children dropped into it are not swallowed (it renders them straight through). Severity: **minor**.
- **Third bug (minor):** `FocusTrapProps` has **no `style` slot at all** — no `StyleSlot`, only `className`, which is not in the registry either. It is the one component of my ten that renders a real layout box (a `<div>`, not `display:contents`) and it is the one that cannot be styled. Padding it out is not authorable.
- **Registry defaults vs the node schema:** `{active:true, autoFocus:true, restoreFocus:true}` → `NodeV2.safeParse` **PASS**, `FocusTrapProps.safeParse` **PASS**. Note there is **no `FocusTrapNode`** in `packages/schema` — it validates only through the `anyRegistered` fallback, whose `props` is `z.record(z.unknown())`, so nothing on this node is prop-checked at the page level. Same for FocusRing, AutoFocus, OptimisticProvider and TourOverlay: five of my ten have no strict node shape.
- **Contract diff:** all three props exposed with correct toggle controls, no dead props, no truncated enums.
- **Missing/desired features:** the `autoFocus`/`restoreFocus` defaults are `true` because that is right at runtime — so the right fix is not to flip them but to give the component a design-time inert mode, exactly as Dialog and InspectorPanel have. Also worth a visible design-time outline: as shipped, the only evidence a FocusTrap is on the page is the hint overlay.
- **Status:** NEEDS_FIX

### Repeat
- **Bugs found:** **A Repeat dropped from the palette is invisible, unselectable and undiagnosable — and it silently invalidates the whole page.** Repro on my own scratch page `/agenta-data-lab-9` (editor `:6501/editor/gh0mlpbp`): palette search "Repeat" → click to insert. Measured three ways.
  1. **No DOM at all.** Before the insert the canvas held `container-yzw923 / stack-u08paz / heading-trnhas`; after it, the same three. There is no `[data-node-id="repeat-2sjl4h"]` element, so `container-yzw923.childElementCount` stays **1**. Re-measured after a full browser reload that re-read the page from disk — still three nodes, still no Repeat element. Not a 0x0 box, not a hairline: **no element is emitted.**
  2. **It is on disk.** `output/gh0mlpbp/src/schemas/agenta-data-lab-9.json` carries `{"id":"repeat-2sjl4h","type":"Repeat","props":{"path":"","as":"item","keyPath":"id"},"children":[]}`. So the drop committed, autosaved, and survives reload. The node exists; only the user cannot see it.
  3. **Nothing else in the editor reveals it.** This editor has no layers/outline panel — the right rail is PROPS / STYLE / BINDINGS / TOKENS and reads "Select a node to edit its style." The only occurrences of the string "Repeat" anywhere in the editor's rendered text are the palette entry's own name and description. There is no empty-node hint, no ghost outline, no error, no console warning. The **only** trace of the node is the JSON file on disk.
  Consequences that follow from (1):
  - **No child can ever be dropped into it.** `resolveAcceptingParent` (frontend/src/components/canvas/hooks/useDrop.ts) walks `targetEl.closest("[data-node-id]")`; with no element there is no drop target, and click-to-insert appends to the selected node or the root — and the Repeat cannot be selected. So the *worst case the brief asks about (a child that persists but never shows) cannot even be reached* — which is not a reprieve, it is a harder wall: the node is inert, permanently.
  - **It cannot be bound.** `source` / `bind` live in the PROPS panel, and the PROPS panel needs a selection.
  **Severity: critical.** Second bug, same node: **a dropped Repeat makes the entire page fail `PageV2`.** `V2RepeatNode.children` is `z.array(NodeV2Ref).min(1)` (packages/schema/src/page.ts:355) while `buildDroppedNode` always ships `children: []`. Verified by parsing the real saved file: `PageV2.safeParse(output/gh0mlpbp/src/schemas/agenta-data-lab-9.json)` → `FAIL — root.children.1.children :: Array must contain at least 1 element(s)`. `anyRegistered` deliberately refuses any type the strict union covers, so there is no fallback. This is exactly the class already logged and fixed for ContextMenu/DropdownMenu/Menubar (`items: z.array(...).min(1)`): one unconfigured node takes every other node on the page out of validated rendering. `/data-lab-7` — another agent's page — is failing for the same reason right now.
  Third: **`bind` is a Properties-panel control that can only break the page.** `repeatEntry` exposes `bind` as a text descriptor, but `V2RepeatNode.props` is `.strict()` and does not declare it — `NodeV2.safeParse({type:"Repeat",props:{bind:"items"},children:[kid]})` → `FAIL — props: Unrecognized key(s) in object: 'bind'`. `bind` is only legal as a **top-level** node key (`{type:"Repeat", bind:"items"}` parses), which the panel cannot write. The registry's own comment already knows this and removed the *seed*; the *control* was left in place. Severity: major.
- **Missing/desired features:** an empty-node placeholder on the canvas — a labelled, selectable, droppable box reading "Repeat — bind a list" — is the whole fix. Everything above follows from there being no box. Also wanted: the `source` field should be a picker over the project's real collections (`items`, `itemStats`) rather than a free-text path, and `path`'s `""` seed should be dropped (absent is the valid state; `z.string().optional()` never wants `""`).
- **Status:** NEEDS_FIX

### Conditional
- **Bugs found:** **Same class as Repeat and slightly worse.** Repro: palette → "Conditional" → click to insert on `/agenta-data-lab-9`.
  1. **No DOM.** Canvas node count before **3**, after **3**. No `[data-node-id="conditional-vq7q7s"]`, no hint, no outline, nothing selectable, and no layers panel to find it in.
  2. **On disk:** `{"id":"conditional-vq7q7s","type":"Conditional","props":{},"children":[]}` — committed and autosaved.
  3. **The page fails `PageV2`,** and for two independent reasons at once. `V2ConditionalNode.props` is `z.object({ when: Expression }).strict()` — **required, no default** — and `children` is `.min(1)`. Measured matrix against the real `NodeV2`: `Conditional props:{} children:[]` → FAIL; `props:{} children:[oneChild]` → **FAIL**; `props:{when:""} children:[oneChild]` → FAIL (`props.when: expression cannot be empty`); only `props:{when:"items"} children:[oneChild]` PASSES. So a Conditional is invalid from the instant it lands and stays invalid until the user types an expression it has no way to type — the `when` control is in the PROPS panel, which needs a selection, which needs a DOM element, which does not exist. Verified on the real saved page: `PageV2.safeParse(...)` → `FAIL — root.children.2.type :: type is already covered by a strict node shape...` (the strict shape rejects it, then `anyRegistered` refuses the type by design, so the reported message is the fallback's, not the root cause). **Severity: critical.**
  4. A latent trap for whoever fixes the above: `Conditional.tsx` (packages/renderer/src/nodes/data/Conditional.tsx) distinguishes "no `when` authored" (renders `children`) from "`when` present and falsy" (renders `else`, which is usually absent → null). So the moment a user types a `when` and then clears it back to `""`, the children **disappear** — the empty string is read as a command ("false"), not as "unset". Same class as the `Sparkline.color: ""` finding. Today this is unreachable because the node cannot be selected; it becomes reachable the day the empty-node placeholder lands.
- **Missing/desired features:** the empty-node placeholder, as for Repeat. Beyond that: the panel exposes only `when` and nothing for the **else** branch, which is half of what a Conditional is — there is no way to author `else` from the editor at all. `props.branches` (the first-match-wins shape the runtime fully supports, and the one the stateful single-page pattern uses) is likewise unreachable and is not even in `V2ConditionalNode`'s strict props, so a hand-authored branches Conditional fails validation.
- **Status:** NEEDS_FIX

### DataBoundary
- **Bugs found:** **Invisible and unselectable on the canvas, like Repeat and Conditional — but it is the one of the four whose node schema is not also broken.** Repro: palette → "DataBoundary" → insert on `/agenta-data-lab-9`. Canvas nodes before **3**, after **3**; no `[data-node-id="databoundary-0iphjh"]`, no hint, nothing to click, no layers panel. On disk: `{"id":"databoundary-0iphjh","type":"DataBoundary","props":{"fallback":""},"children":[]}`. So: committed, autosaved, invisible, unselectable, and therefore un-fillable — `resolveAcceptingParent` has no element to resolve to and click-to-insert cannot target it. **Severity: major** (one notch below Repeat/Conditional only because it does not also invalidate the page).
  Contract check: `V2DataBoundaryNode` has `props` optional and `children: z.array(NodeV2Ref).default([])`, so the dropped node **passes** `NodeV2` and the page still parses — verified against the real saved file, which fails only on the Repeat and the Conditional. `DataBoundary` is also the one of the four that would render correctly if a child could ever be put in it: `DataBoundary.tsx` maps `node.children` straight through with `key = c?.id ?? \`${c?.type}-${i}\``, no gating on data at all.
  Second bug: **`fallback`'s description is false.** The registry says "Text shown while data is loading or empty." `DataBoundary.tsx` renders `props.fallback` in exactly one place — the `catch` of a `try` around child rendering, i.e. only when a child throws synchronously. There is no loading state and no empty state in the component. A user who fills in "Loading orders…" will never see it, and the prop that would make the node visible while empty does not exist. Severity: minor, but it is the prop the whole node is named for.
  Third, minor: `fallback` is seeded `""` where `z.string().optional()` wants absent — the same "empty string is not unset" class as `Sparkline.color`. Harmless here only because nothing reads it on the happy path.
- **Missing/desired features:** the empty-node placeholder (as for Repeat/Conditional). Then the two props the component would need to be worth dropping: a `bind`/`source` the boundary actually watches, and a real `empty` state distinct from the error `fallback`. `V2DataBoundaryNode.props` already declares `bind` and `source` as optional strings — the registry exposes neither, so both are unreachable from the panel.
- **Status:** NEEDS_FIX

### Slot
- **Bugs found:** **A dropped Slot renders `null` unconditionally and warns on every single render pass.** Repro: palette → "Slot" → insert on `/agenta-data-lab-9`. Canvas nodes before **3**, after **3**; no `[data-node-id="slot-1xui5r"]`. On disk: `{"id":"slot-1xui5r","type":"Slot","props":{"name":"default"}}` — committed and autosaved. Unlike the other three this one is *by construction* never renderable in this context: `packages/renderer/src/nodes/slot/Slot.tsx` is four lines — `console.warn` then `return null`. There is no data, no binding and no configuration that makes a Slot draw anything on a page; it only means something when a *layout template* fills it via `applyLayout`. So the palette offers the user a component that can never do anything on the surface they are building. **Severity: major** — and this is the one of the four where "config-driven builtin, renders nothing until bound" is not merely untested but **false in principle**.
  Second bug: **the warning is a console flood.** `read_console_messages` on my tab returned **168** messages, and every one of the first 40 was `[renderer] unfilled slot 'default'` — 4 to 6 per render pass, from a single Slot node, unthrottled, emitted from inside a render body. It is simultaneously the *only* diagnosis the user gets (there is nothing on the canvas) and loud enough to bury every other warning in the console, which is where a developer would look for the real problem. Severity: minor on its own, major in combination with there being no on-canvas signal.
  Contract check: the dropped node **passes** `NodeV2` (`V2SlotNode` wants only `id`, `type` and `props.name: z.string().min(1)`; the seed `"default"` satisfies it), and passes with an added `style` or `children` too, since `V2SlotNode` is a plain `z.object` that strips unknown top-level keys. Note the two Slot schemas disagree: `packages/schema/src/nodes/slot.ts` is `.strict()` at the top level and would reject the `style` the editor's drop path can attach, while `page.ts`'s `V2SlotNode` strips it. The dropped node fails `nodes/index.ts`'s `Node` (v1) and passes `NodeV2` — worth knowing before either is "fixed" to match the other.
- **Missing/desired features:** either (a) hide `Slot` from the palette the way `GridCell` is hidden (`hidden: true` exists on `RegistryEntry` for exactly this — it keeps the type in the registry closure without offering it as something to drag), or (b) give it a visible design-time placeholder — a labelled dashed box reading `Slot: default` — which is what every other template system does and what would make the warning unnecessary. Right now it is the only palette entry that is guaranteed to do nothing.
- **Status:** NEEDS_FIX

#### VERDICT — the four "config-driven builtins" and the excuse in `frontend/src/__tests__/editor-validation.test.tsx`
The excuse — *"config-driven builtins — they correctly render nothing until bound to data"* — **does not hold. P0.** Measured, not inferred, on `/agenta-data-lab-9` with each of the four dropped from the palette and the saved schema re-read from disk:
1. **All four persist and none of the four produces any DOM.** Not a small box, not an empty wrapper: no `[data-node-id]` element is emitted, so `container-yzw923.childElementCount` stays 1 with four extra nodes saved beneath it.
2. **There is no other way to tell they exist.** No empty-node hint, no outline, no error, no layers/outline panel anywhere in this editor. The only trace is the JSON file. `Slot` alone leaves a console warning — and floods 168 copies of it.
3. **"Until bound to data" is unreachable.** Binding happens in the PROPS/BINDINGS panel; the panel needs a selection; selection needs a DOM element to click. **No user can bind any of the four**, so the state the excuse points to cannot be entered from the editor at all.
4. **A child cannot be dropped into any of the three container-typed ones.** Drop resolution walks `closest("[data-node-id]")` and click-to-insert targets the selection; with no element there is neither. So this is *not* the Dialog case (child persists, never shows) — it is a harder wall: the node is permanently inert. That is worse, not better, because the Dialog case at least leaves the user something to be confused by.
5. **Two of the four are not merely invisible, they are invalid.** A dropped `Repeat` (`children` `.min(1)`) and a dropped `Conditional` (`props.when` required, `children` `.min(1)`) each make the whole page fail `PageV2` — proven by parsing the real saved file. `Slot` and `DataBoundary` pass.
The honest statement of the current behaviour is not "renders nothing until bound"; it is **"cannot be seen, cannot be selected, cannot be bound, cannot be filled, and two of them silently invalidate the page they are on."** The test comment should be replaced by a test that drops each one and asserts a selectable design-time placeholder exists.
(Not measurable this session: the scaffold renderer on `:6503` was down for the whole browser phase, so the *runtime* consequence of the two invalid pages — the `[scaffold] schema validation failed … rendering raw` path already documented for the menu nodes — is inferred from the schema result, not observed. Worth re-measuring when `:6503` is back.)

### AutoFocus
- **Bugs found:** **An AutoFocus on the canvas steals the editor's keyboard focus on every page load.** Repro: put a Button inside an AutoFocus on `/overlay-lab-8`, open the editor in a fresh tab, click the page in the pages list, wait for the canvas, and read `document.activeElement` before touching anything: it is `BUTTON` with `innerText === "In AutoFocus"`, `data-node-id="button-af1"`, **inside `[data-canvas-root]`**. Nothing the user did put it there — `AutoFocus.tsx`'s mount effect did, and it wins over the FocusTrap already on the same page (which, having no focusable descendant, only grabs its own root). Expected: a design-time wrapper does not move the editor's caret. Actual: the author's keyboard is inside the artefact they are editing, so the next Space or Enter presses a button on the canvas instead of doing whatever the editor binds it to. Severity: **major**; this is the same class as the three components an earlier round found stealing editor keys, and the same fix applies — the effect must not arm inside `[data-canvas-root]`.
  Worse than a one-off: the effect's deps are `[enabled, selector, delayed]`, so it re-fires whenever any of those three change — i.e. every time the author toggles ENABLED or DELAYED, or types a character into SELECTOR, focus is yanked out of the Properties panel and into the canvas. The `selector` control is a text box, so typing a selector into it is a per-keystroke focus fight.
- **Second bug (minor):** the `delayed` branch is written as `const id = queueMicrotask ? (queueMicrotask(doFocus), null) : setTimeout(doFocus,0)` and the cleanup only clears when `id !== null` — so on the `queueMicrotask` path (i.e. every modern browser) **the focus call cannot be cancelled**. An AutoFocus that unmounts in the same tick it mounted still steals focus. Explains why the theft survives the editor's re-renders.
- **Renders / children / hint:** renders **nothing of its own** — the node is a 0x0 `display: contents` span, by design ("Rendered as a display:contents span so it doesn't affect layout"). It does **not** swallow children: the Button inside it measured 120x40 and was visible. No empty-node hint when it is empty and none is possible (a childless `display:contents` span has no box for `resolveBoxEl` to find), so an empty AutoFocus is invisible and undiagnosable — but it also holds nothing, so nothing is lost.
- **Registry defaults vs the node schema:** `{enabled:true, delayed:true}` (`selector` correctly has no default) → `NodeV2.safeParse` **PASS** — but only via the `anyRegistered` fallback, since **there is no `AutoFocusNode` in `packages/schema`**; its props are never checked at page level. `AutoFocusProps.safeParse` **PASS**.
- **Contract diff:** all three props exposed, controls correct, no dead props, no truncated enums. `className` is declared and *is* destructured (unlike the four floating surfaces). No `style` slot at all, which is defensible for a `display:contents` wrapper.
- **Missing/desired features:** a design-time off switch (the same "inert while editing" affordance Dialog and InspectorPanel have) is the one thing it needs. `enabled: false` is not that — it is the *runtime* setting, so turning it off to stop the editor stealing focus also turns it off in the generated app.
- **Status:** NEEDS_FIX

### FocusRing
- **Bugs found:** **FocusRing draws no ring — the three props it exposes provably cannot change anything, in the editor or in the generated app.** Repro: FocusRing with `color:"#ff0000" width:4 offset:3` wrapping a Button on `/overlay-lab-8`. The custom properties *are* set and *are* inherited — reading the child button's computed style gives `--focus-ring-color: #ff0000`, `--focus-ring-width: 4px`, `--focus-ring-offset: 3px`. Then I scanned **every** CSS rule in the editor document — 246 rules, 0 cross-origin sheets skipped — for `focus-ring` or `forge-focus-ring`: **0 matches**. Nothing consumes the variables, so the focused button's computed `outline-style` is `none`, byte-identical to a Button sitting outside any FocusRing. Severity: **major** — a component whose entire purpose is to make focus visible, that makes nothing visible.
  Cause, and it is not only an editor gap: the rule that reads these tokens lives in `packages/library/src/style/focus-ring.css` and reaches an app only through `backend/services/interactions_css_inject.py`, behind the `FORGE_POLISH_A11Y` flag. I grepped the generated app for it — `output/gh0mlpbp/app/src/` contains **no** `forge:a11y-focus-ring` block and **no** `--focus-ring-color` — so on this project the ring is absent at runtime too. (The app's `tokens.css` does define `--focus-ring`, a *different* variable name that `focus-ring.css` never reads.) So FocusRing is dead in the editor and dead in the shipped app.
- **Renders / children / hint:** renders nothing of its own (0x0 `display: contents` span, by design). Does **not** swallow children — the Button inside measured 118x40 and was visible. No hint when empty; not possible for a childless `display:contents` span, and nothing is lost.
- **Registry defaults vs the node schema:** `props: {}` — correctly, all three descriptors have **no default**, which is right (each falls back to a CSS token). → `NodeV2.safeParse` **PASS**, again only via `anyRegistered`; there is **no `FocusRingNode`**. `FocusRingProps.safeParse` **PASS**.
- **Contract diff:** all three props exposed, controls correct (`color`→color picker, `width`/`offset`→number), no dead props. `className` declared and destructured.
- **Missing/desired features:** the component needs to carry its own rule instead of depending on a flag-gated backend CSS pass — a scoped `<style>` next to the wrapper, or the `[data-forge-focus-ring]:focus-within > *:focus` rule inlined — so that dropping one has a visible effect on the canvas the moment it is dropped. Until then the palette offers an accessibility primitive that does nothing, which is worse than not offering it: an author who drops one believes the page now meets WCAG 2.4.7 and it does not.
- **Status:** NEEDS_FIX

### OptimisticProvider
- **Bugs found:** **`style` is declared in the schema and dropped by the component.** `OptimisticProviderProps` is `.strict()` and includes `style: StyleSlot.optional()`; the TS interface re-declares `style?: StyleSlotT`. `OptimisticProvider.tsx` then destructures `{ resource, toastOnRollback, timeoutMs, children, base, className }` — **no `style`** — and never calls `resolveStyle`. The one prop the schema goes out of its way to type is the one the component ignores. Latent rather than live today (the registry exposes no style descriptor on this entry), but any style written to this node is silently discarded. Severity: **minor**.
- **Second bug (minor):** the registry seeds `resource: ""`, and the component reads `resource ?? "root"` — a nullish check, so `""` is a *value* and beats the fallback; the emitted attribute becomes `data-forge-optimistic=""` rather than `"root"`. Same class as `Sparkline.color: ""` and `ActivityFeed.maxHeight: 0` — a seed the component reads as a command rather than as "unset". The descriptor should simply omit `default`. (Verified live in the other direction: with `resource:"tasks"` authored, the canvas DOM carries `data-forge-optimistic="tasks"`.)
- **Renders / children / focus:** renders nothing of its own — 0x0 `display: contents` div, which is what its own registry description promises ("Layout-neutral (display:contents)"). **Does not swallow children**: the Button inside it measured 172x40 and was visible on the canvas. No keyboard or focus capture of any kind. No empty-node hint when empty, and none possible for a childless `display:contents` div — but since it holds nothing, nothing is lost.
- **Registry defaults vs the node schema:** `{resource:"", toastOnRollback:true, timeoutMs:15000}` → `NodeV2.safeParse` **PASS** (via `anyRegistered`; there is **no `OptimisticProviderNode`**), `OptimisticProviderProps.safeParse` **PASS**.
- **Contract diff:** all three readable props exposed with correct controls; no dead props; `timeoutMs` is a plain number box against `z.number().int().min(0).max(120_000)` — the bounds are not surfaced, so the panel accepts `999999` while `OptimisticProviderProps` would reject it. Minor, but it is the same "control does not know its own schema's constraints" gap seen elsewhere.
- **Missing/desired features:** the honest gap is that **none of its three props can be observed at design time** — there is no pending state, no rollback and no toast on the canvas, so `toastOnRollback` and `timeoutMs` are two controls whose effect the author can never see or check. Given it is a pure context provider, the useful addition is a canvas affordance saying "this subtree is an optimistic scope named X" rather than a zero-height invisible wrapper. `base` (the server-confirmed state the provider reflects) is a component prop with **no Zod field and no descriptor**, so a palette-dropped OptimisticProvider always starts from `{}`.
- **Status:** NEEDS_FIX

### TourOverlay
- **Bugs found:** **`steps` is a one-line text box seeded with `""`, against a Zod field that is a required non-empty array of objects — so every palette-dropped TourOverlay is invalid and renders nothing.** The registry declares `steps: { type: "string", control: "text", default: "" }`; `TourOverlayProps.steps` is `z.array(TourStep).min(1)` with **no default**, where `TourStep` is a `.strict()` `{target, title, body?, placement}`. Building the registry's own default props object and parsing it: `TourOverlayProps.safeParse` **FAILS** — `invalid_type, expected array, received string, path ["steps"]`. It is the only one of my ten whose untouched registry seed fails its own component schema. `TourOverlay.tsx` then does `Array.isArray(steps) ? … : []` and returns `null`, so the node is 0x0 with nothing under it. Exactly the `Select.options` / `Wizard.steps` class the registry has already fixed twice — the comment on `wizardEntry.steps` even spells out the reasoning — and TourOverlay was left behind. Severity: **major** (wrong type, wrong control, and a required prop seeded with a value that can never be valid).
- **Second bug: it is invisible AND undiagnosable in the default state.** With the shipped seed the node measures **0x0**, `display: contents`, and I probed the whole document for the empty-node overlay: **no hint for it anywhere**. Because `NodeV2` has **no `TourOverlayNode`** — it validates only through the `anyRegistered` fallback whose `props` is `z.record(z.unknown())` — nothing upstream rejects `steps: ""` either, and the canvas shows no "invalid node" chip (`[data-invalid-node]` count 0). The page silently carries a component that does nothing and says nothing. The one mitigation: the node does appear in the editor's layer list. Severity: **major**.
- **Third bug: dismissal is permanent, global, and fired by an Escape anywhere in the editor.** With real steps authored the tour does render — measured live at (545,85) 1278x598, filling the canvas, reading "Step one / 1 / 1 / This is the page heading. / Skip / Done" over a `rgba(0,0,0,0.35)` backdrop. I then dispatched a single `Escape` on `window` — the way *any* Escape in the editor arrives, since `TourOverlay.tsx` registers `window.addEventListener("keydown", …)` with no scoping — and read back: the overlay is gone and `localStorage["forge-tour-audit-a"] === "done"`. From that moment it **never renders again on that machine**: a 0x0 node with no hint, and no editor control that clears the flag. An author who presses Escape to dismiss anything else in the editor has silently and permanently hidden their own component from their own canvas. Severity: **major**.
- **Renders / children / editor containment:** `slots: "leaf"`, so nothing can be dropped in and nothing is swallowed. Worth recording as this batch's *good* news: unlike Drawer, TourOverlay's `position: fixed` layers stay **inside the canvas frame** — the backdrop measured exactly the canvas box (545,85,1278,598), not the 1525x678 viewport — because it renders inline rather than through a portal, and the canvas establishes its own containing block. Drawer covers the whole editor precisely because Radix portals it to `document.body`. InspectorPanel's scrim, also inline, likewise measured the canvas box.
- **Contract diff:** all six props have descriptors and none is dead; `storageKey`/`autoStart`/`nextLabel`/`doneLabel`/`skipLabel` defaults all match the component's own parameter defaults, which is right. The failure is entirely `steps`.
- **Missing/desired features:** `steps` wants the `type: "array"`, `control: "json"` treatment its siblings got, with a seeded example step whose `target` resolves to something on a real page — and, better, a target **picker** over the nodes on the current page, since a CSS selector typed by hand against a canvas the author cannot check against is the definition of an unverifiable prop. A design-time "always show step N" toggle, and a way to reset the localStorage dismissal from the panel, are both needed before the component is authorable at all.
- **Status:** NEEDS_FIX

### Table
- **Bugs found:** **The editor exposes 2 of the 23 props `TableProps` declares, and the 21 it hides include every way of putting a row in the table.** Contract diff (tsx introspection of the real Zod shapes, `className`/`style` excluded): `tableEntry` offers `columns` and `caption`. Missing: `rows`, `data`, `title`, `searchable`, `pageSize`, `selectable`, `selectionMode`, `rowActions`, `rowHref`, `bulkActions`, `striped`, `stickyHeader`, `density`, `isLoading`, `skeletonRows`, `reorderable`, `editableColumns`, `editSaveWorkflow`, `emptyText`, `emptyDescription`, `emptyAction`. No dead props, no truncated enums, and the registry defaults **pass** `NodeV2` and `PageV2` on a fresh drop.
  The consequence is measured, not theoretical. A Table dropped on `/agenta-data-lab-9` renders **960x45: a header row and an empty `<tbody>` with `0` rows** — and, worse, **no empty state either**: `tb.querySelector('[data-forge-empty]')` is `null`. The component has a deliberate, well-commented empty state (`<tr data-forge-empty="table">` with an illustration and an `emptyText` headline), and it is gated behind `dataMode = !!records && !children` where `records = Array.isArray(rows) ? rows : Array.isArray(data) ? data : null` (`Table.tsx:362-363`). With `rows` and `data` both absent `records` is `null`, so `dataMode` is false, so the empty state does not render — and the `!dataMode && children` fallback is empty too, because `tableEntry.slots` is `{ type: "leaf" }` and the Table can never be given children either. **Every route to a row is closed**: no `rows` control, no `data` control, no children, and no binding descriptor. So the editor's Table is permanently a header with a void under it, and it does not even say so. **Severity: critical** — this is the flagship data component and it cannot show data.
  Second, minor: `caption` is seeded `""` against `z.string().optional()` — "" rather than absent, the `Sparkline.color` class again. Harmless in practice (`{caption && ...}` guards it) but it is a value where absent is meant.
- **Missing/desired features:** a `rows` binding — the thing a user drops a Table to do. `Table.tsx` already reads a Mustache binding string (`rows="{{items}}"`), the project has real fixtures (`items`, `itemStats`), and `Progress.bind` shows the registry already has a `binding` descriptor type with a `null` "no seed" convention. After that, in rough order of what a user reaches for first: `emptyText`/`emptyDescription` (so the void has words), `title`, `searchable`, `pageSize`, `striped`, `density`, `rowHref`.
- **Verified working:** the `actionPicker` → `type:"array", control:"json"` conversion for `columns`. The panel does not render a raw textarea — it renders a proper row editor (per-row `key` and `label` fields, ↑ / ↓ reorder, ✕ delete, "+ Add row", and an "Edit as JSON" escape hatch). Authored live: renamed column 0's label to `Product`, added a third column `qty`/`Quantity`, set `caption` to `AGENTA CAPTION`. The canvas updated to `["Product","Status","Quantity"]` with a `<caption>`, and `output/gh0mlpbp/src/schemas/agenta-data-lab-9.json` now holds exactly `{"columns":[{"key":"name","label":"Product"},{"key":"status","label":"Status"},{"key":"qty","label":"Quantity"}],"caption":"AGENTA CAPTION"}`. Round-trips to disk. Commit is on blur, not on keystroke.
- **Status:** NEEDS_FIX

### TableSortable
- **Bugs found:** **1. Same void as Table, with no route to a row at all — not even a `rows` prop to add.** `TableSortable.tsx` takes `{ columns, caption, children, onSort, style }` and renders rows *only* from `children`; `tableSortableEntry.slots` is `{ type: "leaf" }`, so the editor can never give it children. Measured on `/agenta-data-lab-9`: 960x90, `ths = ["Name","Status"]`, **`tbody` with 0 rows**, and — unlike Table, which at least *has* an unreachable empty state — TableSortable has no empty branch in its source at all. A dropped TableSortable is a header and nothing, permanently. **Severity: critical.**
  **2. The sort handler is live at design time.** `TableSortable.tsx:48` puts `onClick={() => handleSort(col.key)}` on every `<th>`, with `cursor: pointer`. Clicking the "Name" header on the editor canvas flipped `aria-sort` to `"ascending"`, and a second click to `"descending"` — deterministic, reproduced twice, and the header now reads `"Name ↓"` on the canvas. The user's click on the only visible part of the component mutates component state instead of (or as well as) doing an editor thing, and the sort it performs is over zero rows so nothing else happens. Severity: minor on its own — I could **not** establish that it prevents the node being selected; synthetic-click selection proved unreliable in this harness (a click that selected the plain Table earlier failed to select it later), so I am reporting only the state change I could reproduce, not a selection-blocking claim.
  **3. `onSort` has the wrong control for its type — a JSON object editor pointed at a function.** `tableSortableEntry` declares `onSort: { type: "object", control: "json", default: null }`. The component's `onSort` is `(key: string, dir: "asc"|"desc") => void` and is invoked as `if (onSort) onSort(key, nextDir)`. Anything a user types into that JSON box is a truthy non-function, so the next header click throws `onSort is not a function`. It is inert today only because the `null` default is stripped at drop by `normalizeSeed` — i.e. the prop is safe exactly as long as nobody uses the control the editor offers. Severity: major (a control whose only possible use is a crash). Same shape as the `Repeat.bind` finding: a descriptor left behind after the value it seeded was removed.
  **4. No `className`.** `TableSortable`'s `Props` type declares `style` (used, via `resolveStyle`) but no `className` at all, unlike every sibling in this batch. Node-level `style` still applies through the dispatcher wrapper, so this is not user-visible in the editor; it matters to a producer writing `props.className` on the node, which is silently dropped. Severity: trivial.
  Contract check: registry ↔ component agree on `columns`/`caption`; no node schema exists for `TableSortable` in `packages/schema` (it lands on `page.ts`'s `anyRegistered` fallback), and the registry defaults **pass** `NodeV2` and `PageV2` on a fresh drop.
- **Missing/desired features:** a `rows` prop and binding, as for Table — the component does not have one at all, so this is a library change, not just a registry one. `sortable` per column is already honoured in the column shape but not surfaced by the row editor's fields (only `key` and `label` are offered), so the one thing this component does differently from Table cannot be configured. A `defaultSortKey` / `defaultSortDir` would be the obvious pair to add alongside it.
- **Status:** NEEDS_FIX

### DataGrid
- **Bugs found:** **`className` and `style` are declared on `DataGridProps` and destructured by neither.** `DataGrid.tsx:24-31` destructures `{ columns, rows, rowKey, virtualise, selectable, rowActions }` and stops; `DataGridNode.props` (packages/schema/src/nodes/data-display.ts) declares `className: z.string().optional()` and `style: z.record(z.unknown()).optional()`. A producer that writes either onto the node's props has them silently discarded. **Not user-visible in the editor** — I set WIDTH via the Style panel on a sibling node and confirmed the dispatcher applies node-level style to the `<span data-node-id>` wrapper (`width: 400px; max-width: 960px; min-height: 200px`, and the child `<ol>` followed) — so the Style panel still works on a DataGrid. Severity: trivial/informational, logged because the brief asks for it, not because a user hits it.
  Second, minor: `expandable` is offered in the panel (`boolean/toggle`, default `false`) and `DataGrid.tsx` does not destructure it either — the toggle writes a prop nothing reads. It is in `DataGridProps` and in `DataGridNode`, so it is not a dead *contract*, just a dead *control*. Severity: minor (an editor affordance that does nothing).
  No other contract gaps: every other registry prop maps to a real component prop, no dead props, no truncated enums, and the registry defaults **pass** `NodeV2` and `PageV2` on a fresh drop.
- **Missing/desired features:** none material. `rows` is honestly typed (`union(string | array<record>)`) so a `"{{items}}"` binding is expressible through the JSON control today, which is more than Table can say. If anything, a first-class binding descriptor for `rows` (as opposed to typing a Mustache string into a JSON box) is the obvious next step, and the same one Table needs.
- **Verified working:** the `array/json` conversions for `columns` and `rows`. Dropped on `/agenta-data-lab-9` it renders **960x180 with a real header (`ID / Name / Status`) and two seeded body rows (`1 First row Active`, `2 Second row Pending`)** including the row-action affordance — i.e. the seeds are authored well enough to show the user what the component is for, which is the point of seeding them. React keys are safe: `rowId = String(row[rowKey] ?? index)`, so rows missing an `id` fall back to the index rather than colliding. `read_console_messages` over the whole session returned **zero** React key warnings, zero "Encountered two children with the same key", zero prop-type warnings.
- **Status:** NEEDS_FIX

### Timeline
- **Bugs found:** **`className` and `style` are declared and never destructured.** `Timeline.tsx:15` is `export function Timeline({ entries, orientation = "vertical" }: Props)`, where `Props = z.infer<typeof TimelineNode>["props"]` and `TimelineNode.props` declares both `className` and `style`. Same class as DataGrid, same mitigation: node-level style still lands on the dispatcher wrapper — measured directly on this node, setting WIDTH to `400px` in the Style panel took the wrapper from **960px to 400px** with inline `width: 400px; max-width: 960px; min-height: 200px`, and the inner `<ol>` followed to 400px. So the Style panel is unaffected; only a producer writing `props.style` is. Severity: trivial/informational.
  That is the whole list. This is the cleanest component in the batch alongside EditableLineGrid.
- **Missing/desired features:** the entries row editor exposes the same generic `key`/`label` pair shape as the other array controls; a `TimelineEntry` is `{ timestamp, title, actor?, detail?, status? }`, and `status` in particular is a five-value enum (`pending / approved / rejected / info / completed`) driving the dot colour, which a user has no way to discover from a generic row editor. A per-descriptor field schema (so the array control renders the *right* fields with the *right* controls) is the general form of this, and it applies to `Table.columns`, `DataGrid.columns/rows` and `EditableLineGrid.columns` equally.
- **Verified working:** the `entries` conversion to `type:"array", control:"json"`. Dropped on `/agenta-data-lab-9` it renders **960x200, an `<ol>` with 2 `<li>` entries** — `1/1/2026, 2:30:00 PM / Created` and `1/2/2026, 2:30:00 PM / Approved / Jane Doe` — with the status dots resolved (`bg-emerald-500` for `completed`). Both `orientation` values are exposed and the enum is complete (2 of 2). The previously-fixed React key issue is confirmed fixed at the source (`key={e.id ?? i}`, both the vertical and horizontal branches) and confirmed empirically: **zero** key warnings in the console across the whole session. Registry defaults **pass** `NodeV2` and `PageV2` on a fresh drop.
- **Status:** NEEDS_FIX

### Chart
- **Bugs found:** **Six props the component reads are unreachable from the editor**, and they are the six that were added to give a chart a header and a second encoding: `title`, `help`, `overlay`, `viewToggles`, `encoding`, `semanticColor`. `ChartNode.props` (packages/schema/src/nodes/charts.ts) declares all six and `Chart.tsx` passes the whole props object down to the per-type impls; `chartEntry` offers only `chartType`, `data`, `series`, `xKey`, `height`, `showGrid`, `showLegend`, `showTooltip`. `title` and `help` are the ones a user will look for first — a dashboard chart with no way to give it a name is the complaint that writes itself. Severity: major (a documented, implemented feature set with no route to it).
  No dead props, no truncated enums — `chartType` offers all seven of `line / bar / area / pie / donut / funnel / radar`, matching the schema's enum exactly. Registry defaults **pass** `NodeV2` and `PageV2` on a fresh drop.
  Minor, and worth knowing before anyone "fixes" it: `ChartNode.props.chartType` is **required** (not optional, no default) while the registry seeds `"line"`, so the seed is load-bearing — removing it as a "harmless default" would make every dropped Chart invalid. The seed is correct as it stands.
- **Missing/desired features:** `title` + `help` (the header row the schema already describes), then `overlay` and `viewToggles`. Also: `data` and `series` are edited as raw JSON and must agree with each other (`series[].dataKey` has to name a key present in every `data` row) with nothing checking that, and nothing binding either to the project's real fixtures.
- **Verified working:** the `actionPicker` to `array/json` conversion for both `data` and `series`. Dropped on `/agenta-data-lab-9` the Chart renders a **real 960x300 SVG with 2 `<path>` elements, an x-axis reading `Mon Tue Wed Thu`, a y-axis reading `0 6 12 18 24`, and a `Value` legend** — the seeded `data` and `series` agree with each other and produce a chart that looks like a chart, which is the whole reason for seeding them. `data-forge-empty` / `data-chart-placeholder` is **absent**, i.e. this is the real render, not the "no fixture data" placeholder. `className`/`style` are declared and reach the impls via the props spread. No console warnings of any kind.
- **Status:** NEEDS_FIX

### EditableLineGrid
- **Bugs found:** none.
  Contract check: registry and `EditableLineGridProps` agree on all eight props — `columns`, `rows`, `rowKey`, `showLookup`, `lookupPlaceholder`, `removable`, `totals`, `emptyMessage`. No props missing from the editor, no dead props, no enums to truncate, no required prop without a default, and every control matches its type (`array` to json, `object` to json for `totals`, `boolean` to toggle, `string` to text). Registry defaults **pass** `NodeV2` and `PageV2` on a fresh drop. No node schema exists for this type in `packages/schema`, so it lands on `page.ts`'s `anyRegistered` fallback, which is correct behaviour rather than a hole.
  One cosmetic mismatch, not a bug: the registry seeds `emptyMessage: "No line items."` while the component's own parameter default is the longer `"No line items. Use the lookup above to add one."`. The seed wins, which is what a seed is for; the shorter text is the better one when `showLookup` is `false`, which is also the seeded state. Consistent, so not logged as a defect.
- **Missing/desired features:** the column row editor offers generic `key`/`label` fields, but an `EditableLineGrid` column also carries `type` (`text` / `number` / `currency` / `select`), `options`, `align`, `width` and `placeholder`, and `type` is what decides whether a cell is a text box, a number box or a dropdown. Same per-descriptor-field-schema ask as Timeline. Beyond that: `totals` is edited as raw JSON (`{ auto, taxLabel, taxRate, currency, ... }`) where a small form would do, and there is no binding for `rows`.
- **Verified working:** dropped on `/agenta-data-lab-9` it renders **960x387** — a real header (`Item / Qty / Price`), **one seeded body row whose three `<input>` elements carry the seeded values `"Line item"`, `"1"`, `"0"`** (the values live in `input.value`, not in `innerText`, which is why a text-only probe reads the row as blank), a `data-row-key="1"` on the row, and a totals block reading `Subtotal 0.00 / Total 0.00`. Row keys are safe (`row[rowKey] ?? rowIdx`). `className` and `style` are both destructured and applied (`className={["w-full", className]...}`). Zero console warnings.
- **Status:** CLEAN

### CartPanel
- **Bugs found:** **Three of the six props the editor exposes cannot affect anything a user can see, because the branch that reads them is behind a non-empty cart that the editor can never produce.** Dropped on `/agenta-data-lab-9` the CartPanel settles (after a brief `data-cart-panel="loading"` first paint) at **960x520 rendering `Your cart` / `0 items` / `Your cart is empty.`** — and `cp.querySelectorAll('button').length` is **0** and `cp.querySelectorAll('select').length` is **0**. So the checkout footer is not in the DOM at all, and with it go `checkoutLabel` (seeded `"Place order"`), `paymentMethods`, and `onCheckoutNavigate` (seeded `"/orders"`). A user who renames the button to "Buy now" gets no feedback of any kind, ever, from the editor. Severity: major — half the panel's configuration surface has no preview. This is the same shape as the already-fixed CartBadge finding (a design-time render that depends on a `/api/cart` the editor does not have), but the fix here is different: CartBadge needed an honest zero state, CartPanel *has* an honest zero state and needs a way to preview the non-empty one.
  Contract check is otherwise clean: registry and `CartPanelProps` agree on all six props (`title`, `emptyState`, `checkoutLabel`, `currency`, `paymentMethods`, `onCheckoutNavigate`), no props missing from the editor, no dead props, no enums to truncate, no required prop without a default. `className` is destructured and applied. Registry defaults **pass** `NodeV2` and `PageV2` on a fresh drop, and `paymentMethods`'s `default: null` is correctly stripped by `normalizeSeed` rather than written as a literal `null`.
  Second, minor: `currency` is seeded `"USD"` against `z.string().optional()`. Defensible (a currency has to be *something* to format against) but it is a design-time decision baked into every dropped panel, and there is no way to express "inherit from the app's locale".
- **Missing/desired features:** a design-time preview of the filled cart — the single change that would make the other three props editable in any meaningful sense. A `previewItems` prop, or the same design-time-fixture mechanism `Table`/`DataGrid` want for `rows`, would do it. Also: `onCheckoutNavigate` is a free-text route with no picker over the project's real routes — the same gap already routed to frontend for `Redirect.to` / `Link.navigate` / `NavLink.navigate` / `SkipLink.target`; add this one to that list.
- **Status:** NEEDS_FIX

#### AGENT A — batch notes (contract sweep + environment)
- **Node-schema check, 11 of 11 measured.** A dropped node was rebuilt exactly as `buildDroppedNode` builds it (registry defaults through `normalizeSeed`, the `name` auto-seed, the drop `style`, `children: []` for non-leaf types) and `safeParse`d against the real `NodeV2` and `PageV2` from `packages/schema/src/page.ts`. **9 pass, 2 fail: `Repeat` and `Conditional`** — both because their strict shapes require something a fresh drop cannot have (`children.min(1)`, and for Conditional a required non-empty `props.when`). The failures were then confirmed against the actual on-disk page rather than a reconstruction: `PageV2.safeParse(output/gh0mlpbp/src/schemas/agenta-data-lab-9.json)` reports exactly those two nodes.
- **Controls converted from `actionPicker` to `type:"array", control:"json"` — all six verified in the panel.** `Table.columns`, `DataGrid.columns/rows`, `Chart.data/series`, `Timeline.entries`, `EditableLineGrid.columns/rows` all render a structured row editor (per-row fields, reorder, delete, add, plus an "Edit as JSON" escape hatch) rather than a raw textarea, and all six produce a render that matches the seed. Round-trip proven end to end on `Table.columns`: authored in the panel, updated on the canvas, and correct in the saved JSON on disk.
- **React key warnings: none.** `Table` (`rowId(r, i)`), `DataGrid` (`String(row[rowKey] ?? index)`), `EditableLineGrid` (`row[rowKey] ?? rowIdx`) and `Timeline` (`e.id ?? i`) all carry index fallbacks. `read_console_messages` across the session found zero key warnings, zero duplicate-key errors and zero prop-type warnings. `CartPanel` is the one row map with **no** fallback (`key={r.id}`), which is only safe as long as `/api/cart` always returns ids — not reachable from the editor, so not logged as a bug.
- **Console noise:** 168 console messages were captured on my tab and the overwhelming majority are `[renderer] unfilled slot 'default'`, 4-6 per render pass from a single Slot node. Anyone reading this console for a different bug will not find it. Logged under Slot.
- **Environment, for whoever reads this next.** The `:6501` editor went down three separate times during the browser phase — a Next dev-server manifest race (`SyntaxError: Unexpected end of JSON input` at `loadManifest`), then `Attempted import error: 'DesignTimeProvider' is not exported from '@tentoroforge/library'` from `Canvas.tsx` which blanked the whole canvas with `Element type is invalid`. Both cleared on their own; `DesignTimeProvider` **is** exported from both `packages/library/src/index.ts` and `dist`, so that was a stale bundle mid-rebuild, not a real break. Not logged as component bugs. **`:6503` (the render scaffold) was down for the entire browser phase**, so the runtime consequence of the two `PageV2`-invalid pages — the `[scaffold] schema validation failed ... rendering raw` path already documented for the menu nodes — is inferred from the schema result and **not** independently observed. Worth re-measuring.
- **A false positive I caught and am not reporting**, so nobody re-chases it: the array row editor commits on `focusout`, and React listens for `focusout` rather than `blur`. A synthetic `FocusEvent('blur')` therefore looks exactly like "the panel discards what you type". It does not — with a real focus change every field committed to the store and to disk. Related: synthetic-click node selection proved unreliable in this harness (the same click that selected a node early in the session failed to select it later), so no claim in my entries rests on a selection that did not happen.
- **Scratch page:** `/agenta-data-lab-9` (`output/gh0mlpbp/src/schemas/agenta-data-lab-9.json`) is mine and holds all 11 components; it is currently `PageV2`-invalid *because of* the Repeat and Conditional findings, which is the evidence. Leave it as it is until those two are fixed, then it doubles as the regression check.
