// The account entity's table, for signup to write the account's own row —
// projected from the Living Blueprint (services/blueprint/account_model.py).
// Server-only: it imports the database schema.
//
// This default is an application with no account entity.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const accountTable: any = null;
