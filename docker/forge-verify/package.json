{
  "name": "forge-verify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-Verify Pass runner — Playwright + Fastify HTTP API",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p .",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/sensible": "^5.6.0",
    "playwright": "1.48.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
