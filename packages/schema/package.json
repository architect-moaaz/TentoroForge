{
  "name": "@tentoroforge/schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "emit:blueprint-schema": "tsc scripts/emit-blueprint-schema.ts --outDir .schema-emit --module commonjs --moduleResolution node --target ES2022 --strict --skipLibCheck --esModuleInterop && node -e \"require('fs').writeFileSync('.schema-emit/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\" && node .schema-emit/scripts/emit-blueprint-schema.js && rm -rf .schema-emit"
  },
  "dependencies": {
    "zod": "^3.25.0",
    "zod-to-json-schema": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.1"
  }
}
